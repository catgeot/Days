# gateo.kr 프로젝트 컨텍스트 (AI Context)

## 1. 프로젝트 개요
- **서비스명**: gateo.kr
- **설명**: React 기반의 모던 웹 애플리케이션으로, Supabase를 백엔드로 사용하며 3D 시각화 및 AI 기능을 통합한 서비스입니다.

## 2. 주요 기술 스택
- **Frontend**: React 19.2.0, Vite, React Router DOM
- **Styling**: Tailwind CSS, Lucide React
- **Backend & BaaS**: Supabase (`@supabase/supabase-js`)
- **3D & Data Visualization**: `react-globe.gl`, `three`, `@react-three/fiber`, `@react-three/drei`
- **AI Integration**: `openai`, `@google/generative-ai`
  - **사용 가능한 Gemini 모델 목록 (2026.03.12 기준)**:
    - `gemini-3.1-pro` (텍스트 출력 모델)
    - `gemini-2.5-pro` (텍스트 출력 모델)
    - `gemini-2.5-flash` (텍스트 출력 모델)
    - `gemini-2.5-flash-lite` (텍스트 출력 모델)
    - `gemini-3.1-flash-lite` (텍스트 출력 모델)
    - 기타 모델들은 API 콘솔 참조
- **State Management**: React Context API
- **Deployment**: Vercel

## 3. 폴더 구조 및 아키텍처
- `src/components/`: 재사용 가능한 UI 컴포넌트
- `src/pages/`: 라우팅되는 페이지 단위 컴포넌트
- `src/context/`: Context API를 활용한 전역 상태 관리 로직
- `src/shared/`: 공통 유틸리티, 커스텀 훅, 상수 등
- `supabase/`: Supabase 관련 설정, 데이터베이스 스키마 및 마이그레이션

## 4. 🤖 AI 어시스턴트 핵심 행동 원칙 (Core Rules)
사용자의 요청에 따라 코드를 수정하거나 기능을 추가할 때 다음 프로세스를 **엄격하게 준수**합니다:
1. **논리적 구조 제안**: 수정 및 구현하려는 코드의 논리적 구조와 방향성을 먼저 설명합니다.
2. **사용자 승인**: 제안한 구조에 대해 사용자의 승인을 대기합니다.
3. **직접 파일 수정(Edit Tool 사용)**: 승인이 완료되면 채팅창에 전체 코드를 길게 출력하며 토큰을 낭비하는 대신, AI가 에디터의 파일을 직접 수정(Edit)하여 신속하게 변경사항을 반영합니다.
4. **디자인 임의 변경 금지 및 UI 밀도 원칙**: 모바일, 태블릿 대응 등 반응형 디자인 최적화를 포함하여 기존 UI/UX 디자인을 사용자의 명시적인 요청이나 합의 없이 일방적으로 변경하지 않습니다.
   - **스크롤 최소화 및 중요 버튼 누락 방지 원칙**: 장소 리뷰 등 작성하기/편집창 뷰를 구성할 때 전체 스크롤을 최소화(Scroll-Free)하여, 공간 효율성을 위해 패딩/글자크기/버튼크기를 최적화하고 한 화면에 정보를 담아야 합니다. (예: 전체공개 버튼이나 등록 버튼이 스크롤 아래에 숨어 의도치 않게 확인 없이 등록되는 문제를 방지할 것).
   - **타이포그래피 및 단어 끊김 방지 원칙**: 한국어 텍스트가 렌더링되는 문단(`p`, `div` 등)에는 단어가 중간에서 무분별하게 끊겨 줄바꿈되는 현상을 방지하기 위해, 가독성을 고려하여 반드시 CSS 속성(`word-break: keep-all;` 또는 Tailwind 클래스 `break-keep`)을 명시적으로 적용해야 합니다.
5. **백엔드 로직 수정 및 배포 의무**: Supabase Edge Function이나 DB 스키마 등 백엔드 영역의 코드를 수정한 경우, AI는 반드시 터미널을 통해 클라우드에 업데이트(배포)해야 반영됩니다. `.env.local` 등에서 Project Ref ID를 찾아 `npx supabase functions deploy <function_name> --project-ref <ID>` 명령어로 **AI가 직접 배포를 우선 시도**해야 하며, 인증 문제로 실패할 경우에만 사용자에게 명령어 복사/실행을 친절히 안내합니다.
6. **데이터 구조 및 Hydration 규칙 엄수**: DB에서 불러온 동적 장소 데이터(`savedTrips` 등)는 장소명이 `destination` 컬럼으로 관리되나, `PlaceCard` 등의 UI 컴포넌트는 전역 상태에서 `location.name` 속성을 필수로 요구합니다. 라우팅 로직이나 컴포넌트 상태 설정(`setSelectedLocation` 등) 시, **반드시 원본 DB 객체를 복사한 후 `name: target.destination` 등으로 필수 UI 속성을 매핑(Hydration)** 하는 과정을 거쳐야 합니다. 누락 시 장소 카드가 빈 화면으로 렌더링되는 치명적 버그가 잦게 재발하므로 데이터 흐름 변경 시마다 각별히 주의해야 합니다.
7. 🚨 **갤러리(이미지 검색/페이지네이션) 로직 수정 시 절대 주의**: 
   - 사진 갱신(Refresh) 로직이나 검색어 정제 로직을 변경할 때, **기존에 잘 나오던 사진이 갑자기 0개가 되거나 빈약해지는 문제(regression)가 수차례 반복**되었습니다. 
   - API 검색어 최적화 시 오지/자연경관(예: 에베레스트, 통가) 등은 한글 검색으로 결과가 나오지 않으므로 영문 매핑(`FALLBACK_DICTIONARY`)을 함부로 제거하지 말고 보존해야 합니다.
   - '강제 새로고침(Force Refresh)' 등 페이지네이션 로직을 짤 때는 새로운 결과를 기존 결과에 덮어씌워버리면(Replace) 사진 수가 줄어들어 빈약해 보이게 되므로, 반드시 **기존 사진들에 새 사진들을 누적(Append)** 하는 방식을 유지해야 합니다.
8. **작업 단위 분할 및 커밋 주도 원칙 (Commit-Driven Workflow)**: 작업 효율성을 위해 한 번에 여러 파일이나 여러 기능을 변경하는 것은 허용하지만, 사후 리뷰와 이력 관리가 어려워지지 않도록 **논리적 단위(하나의 기능 완성 또는 하나의 주요 파일 완성)** 로 작업 단계를 명확히 분할하여 순차적으로 진행합니다. 너무 많은 이질적인 기능들을 한 번에 무더기로 수정하고 결과를 통보하는 것을 지양합니다. 하나의 단위 작업이 완료될 때마다 사용자에게 **점검해야 할 사항(테스트 포인트)** 과 해당 변경 사항에 대한 **`git commit -m "커밋 메시지"`** 를 제안하여 사용자가 즉각적으로 버그 확인 및 커밋을 진행할 수 있도록 돕습니다.
9. **한글(영문) 고유명사 표기 규칙**: AI가 장소나 상호명 등 고유명사를 생성하거나 답변할 때는 항상 `한글명('현지 영문명')` 형식을 엄수합니다. 스마트 툴킷이나 로컬 가이드 텍스트 등에서 이 표기법을 통해 검색의 일관성과 가독성을 높입니다.

## 5. 최근 수정 사항 및 남은 과제

### 🔥 진행 완료된 최신 작업
- **단일 소스 원칙(Single Source of Truth) 기반 위키-툴킷 통합 (Phase 4 완료)**:
  - 위키 정보(`ai_practical_info`)를 유일한 원본 데이터로 삼아 백엔드(`update-place-wiki`)에서 `essential_guide` JSON 별도 생성을 폐기하고, 단일 마크다운 안에 구분자(`---[TOOLKIT_START]---`)를 삽입하여 구조화.
  - 프론트엔드 단에서 `aiDataParser.js` 유틸리티를 신설하여 단일 마크다운을 파싱, 위키 읽기 본문과 툴킷 카드 데이터로 분리하여 효율적으로 렌더링. 구버전 데이터 호환성을 위한 폴백 로직 완비.
  - 툴킷 UI 카드 순서를 여행자의 사고 흐름(비자->항공->숙박->이동->지도/안전)에 맞춰 재배치하여 사용성 극대화.
- **위키 본문 스마트 아웃링크 적용 (Phase 4.5 완료)**:
  - `ToolkitTab.jsx`에 있던 `CopyableText` 파싱 로직을 공통 컴포넌트(`src/components/PlaceCard/common/CopyableText.jsx`)로 분리.
  - `PlaceWikiDetailsView.jsx` 본문에 해당 공통 컴포넌트를 적용하여, 위키 글을 읽는 도중 고유명사를 클릭하면 즉시 스마트 아웃링크 검색이 되도록 경험 개선.
- **메인 네비게이션 압축 및 갤러리 토글 방식 도입 (Phase 5 일부 완료)**:
  - 메인 네비게이션을 '리뷰, 위키, 영상' 3개 탭으로 압축하여 공간 효율성 극대화.
  - 리뷰/위키/영상 탭 활성화 상태에서 해당 탭을 한 번 더 누르면 즉시 '갤러리' 모드로 복귀하는 토글 스위치 도입 및 버튼 정렬 최적화 (모바일 중앙, PC 우측).
- **비로그인 사용자 인증 유도 UI 추가 (Phase 6 완료)**:
  - `Sidebar.jsx` 하단에 비로그인 전용 안내 박스("나만의 여행 기록을 남겨보세요!") 및 로그인 페이지 진입 버튼 추가하여 신규 사용자 가입 유도 강화.

- **위키/스마트 툴킷 갱신 로직 안정화 및 UI 개선 (Phase 6 일부 완료)**:
  - 비용 절감을 위해 넣었던 'Defensor(방어) 로직'이 오히려 포맷 불일치 버그를 유발함을 파악, 엣지 함수(`update-place-wiki`) 프롬프트에서 완전히 걷어내고 항상 템플릿 구조를 따르도록 단일화.
  - 14일 초과 시 자동 갱신 트리거 로직을 `WIKI_AUTO_UPDATE_DAYS` 공통 상수로 분리.
  - 위키 및 툴킷 탭 상단에 '수동 강제 갱신' 버튼을 추가하여 생성되지 않은 정보나 테스트 시 즉각적인 확인이 가능하도록 조치.

- **스마트 툴킷 종합 최적화 (Phase 6 완료 - 2026.03.30)**:
  - **스마트 링크 통합**: 구글 맵/웹 검색 분리 제거, 모든 링크를 구글 검색으로 통합하여 일관된 사용자 경험 제공.
  - **로딩 동기화 개선**: 툴킷 탭 진입 시 wikiData가 없으면 자동으로 데이터 요청, 위키 탭을 먼저 거치지 않아도 정상 작동하도록 개선.
  - **카드 순서 재배치**: 여행 플래닝의 자연스러운 흐름에 맞춰 순서 변경 (지도 → 비자 → 항공 → 숙박 → 유심 → 교통 → 앱 → 안전).
  - **카테고리별 색상 테마**: 8가지 색상 팔레트 적용으로 시각적 구분 강화 (emerald, blue, sky, purple, teal, green, amber, red).
  - **UI/UX 개선**: 로딩 메시지에 이모지 추가, 모바일 터치 영역 최적화 (최소 44px), aria-label 접근성 개선.
  - 5개의 순차적 커밋으로 관리 용이성 확보.

### 📦 이전 주요 작업 내역 (Archive)
- **코어 UI & 시각화**: 80개 실제 목적지 기반 3D 지구본 마커 균일화 및 렌더링 최적화, DailyReport 밝은 테마 적용, 영문 `slug` 기반 SEO URL 라우팅 도입.
- **이미지 갤러리**: Pexels/Unsplash API 연동 및 Fallback 전략, 검색어 정제, 사진 갱신(Append) 페이지네이션 및 사용자 좋아요 기반 큐레이션.
- **리뷰 시스템 (Logbook)**: `place_reviews` 테이블 신설, 사용자 리뷰 및 평점 시스템 연동, AI 리뷰 초안 생성 기능, 비로그인 퍼블릭 뷰어 및 블로그(여행기) 가로 스크롤 카드 연동.
- **AI 백과 (Wiki) 및 툴킷**: 백그라운드 AI 응답(Polling) 동기화, 제휴 링크(Travelpayouts) 연동 및 다중 링크 버튼 지원. 스마트 링크(구글 맵/웹 자동 파싱) 도입 및 텍스트 정규화 로직(스마트 따옴표 방어) 적용.

### 🚀 남은 과제 (Next Steps)
- [ ] **[Phase 7] 스마트 툴킷 버그 수정 및 최종 개선 (다음 세션)**
  - **Critical**: 툴킷 탭 직접 진입 시 로딩 상태가 지속되는 문제 해결 (폴링 로직 강화 필요).
  - **Medium**: 콘솔 로그 최적화 - aiDataParser 중복 경고 제거, Production 로그 필터링.
  - **Medium**: 툴킷 하단 '강제 갱신' 버튼 제거 또는 관리자 전용 숨김 처리.
  - **Low**: 툴킷 항목별 가독성 개선 - 불필요한 기호 제거, 줄바꿈 최적화, 타이포그래피 조정.
  - **Optional**: 백엔드 프롬프트 개선으로 AI 출력 품질 향상.
  - 상세 계획: `plans/toolkit-optimization-phase7-next-session.md` 참조.
- [ ] **[Future] 갤러리 통합 개편 및 사용자 사진 업로드**
  - 브라우저 단 이미지 압축(`browser-image-compression`) 적용, Supabase Storage 연동을 통한 사용자 사진 업로드 폼 신설.
  - 다중 API(Pexels, Unsplash, 사용자 사진) 출처 워터마크 표시 고도화 및 사진 큐레이션 방식 개편.

---

## 🚀 최종 목표 (Vision & Roadmap)
**🔥 초개인화 AI 여행 가이드 비서 앱 (Mobile App)**
현재의 웹 기반 여행 정보 제공 서비스(gateo.kr)를 넘어, **"여행 관리의 최전선에서 여행자의 일정을 A to Z로 책임지고 동행하는 스마트 가이드 앱"**으로 진화하는 것이 궁극적 로드맵입니다.

- **개인 일정 동기화 (Import)**: 스마트 툴킷을 통해 선별된 항공권, 호텔, 렌터카, 페리, 교통 패스, 관광 일정 데이터를 사용자의 개인 여행 캘린더/일정으로 가져오는 기능.
- **오프라인 햅틱 관리**: 네트워크가 단절된 비행기 안이나 오지에서도 타이머 및 오프라인 저장소를 활용하여, 비행기 탑승, 호텔 체크인/아웃, 공항 픽업 등 주요 크리티컬 일정을 놓치지 않도록 알림 및 관리.
- **실시간 컨텍스트 어웨어 (Context-aware)**: 온라인 연결 시, 사용자의 현재 위치와 스케줄을 파악하여 주변의 꿀팁이나 맞춤형 툴킷 정보를 실시간 푸시로 안내.
