# gateo.kr 프로젝트 컨텍스트 (AI Context)

**최종 업데이트**: 2026-05-21 (페리 노선·여행지 추가 체크리스트)  
**사용**: Cursor 등 새 세션에서 **`@.ai-context.md`를 연결**하면 이 저장소 작업에 맥락이 맞습니다. (대화·세션 간 **자동 유지되지는 않습니다**.)

**최신 일지(참고)**: [`plans/2026-05-21-project-log.md`](plans/2026-05-21-project-log.md) — 직전: [`plans/2026-05-19-project-log.md`](plans/2026-05-19-project-log.md) · [문서/아카이브 README](plans/README.md)

---

## 1. 프로젝트 개요

- **서비스**: [gateo.kr](https://www.gateo.kr) — **Vite + React(19) SPA**, React Router, Supabase BaaS, 3D 글로브·Mapbox, AI·제휴(트립·페리·툴킷) 통합
- **SEO/봇**: `index.html` 정적 콘텐츠 블록, `react-helmet-async`, Vite sitemap(여행지·explore URL 다수), GA4는 SPA `useLocation` 추적

---

## 2. 주요 기술 스택

| 구분 | 기술 |
|------|------|
| 빌드 | Vite 7, `@vitejs/plugin-react`, (로컬 HTTPS) basic-ssl |
| UI | React 19, React Router 7, Tailwind 3, Lucide |
| 데이터 | Supabase JS, Edge Functions (Deno) |
| 3D/지도 | `react-globe.gl`, three, R3F, `mapbox-gl` / `react-map-gl` |
| AI | `@google/generative-ai` / OpenAI (클라이언트 직접 키 호출 금지 **→ `gemini-proxy` 등** 경유) |
| 배포 | Vercel, `@vercel/analytics` |

---

## 3. 폴더 구조 (요약)

- `src/components/` — 재사용 UI (PlaceCard·모달 등)
- `src/pages/` — Home, DailyReport(블로그) 등 **라우트 단위** 페이지
- `src/context/` — `ReportContext` 등 **Context API** 전역 상태
- `src/pages/DailyReport/context/` — **`PenNameProvider`** / `usePenNameContext` (로그북 필명, `profiles.display_name` 단일 소스)
- `src/shared/` — **Auth, MainLayout, 공용 레이아웃/인증** 중심 (훅·유틸은 `src/hooks/`, 각 기능 `hooks/`에 분산)
- `supabase/` — Edge Functions, DB 관련
- `scripts/` — 데이터 스크립트
- `plans/README.md` — **문서/일지** 규칙과 아카이브 안내
- [`plans/travel-spots-management.md`](plans/travel-spots-management.md) — **여행지 추가·도착 공항** 운영 가이드(대량 추가 시)
- [`plans/klook-rental-search-data.md`](plans/klook-rental-search-data.md) — **클룩 렌터카 배너 검색어**·예외 JSON 스키마(여행지명 우선, 공항 DB 완료 후 예외 추가)

### 꼭 기억할 시스템 (토큰·장애 방지)

- **🚨 여행지 목록만 확인할 때**: **반드시** [`src/pages/Home/data/travelSpots-list.json`](src/pages/Home/data/travelSpots-list.json) (경량) 사용. [`travelSpots.js`](src/pages/Home/data/travelSpots.js)는 6,000+ 줄으로 **검토용 비권장**.  
  - 재생성: `node scripts/extract-travel-spots-list.cjs`
- **AI 모델**: 위키/툴킷 **역할 분리** (2.5 Pro / 3.1 Pro + 폴백). 프롬프트·키는 **에지 프록시** 쪽 정책을 따름.
- **Mapbox** 등: 토큰은 **URL 제한·서버/프록시** 관점의 보안 가이드 준수
- **SEO 라우트**: `/place/:slug` 및 `/place/:slug/:tab`, `vite-plugin-sitemap` + 동적 URL 목록
- **PlaceCard + Supabase/DB `destination`**: `PlaceCard`에 넣기 전 **객체 복사 후** `name` 등 **hydration** — 미매핑 시 **빈 UI**로 이어짐
- **갤러리** (`usePlaceGallery`): `FALLBACK_DICTIONARY` **임의 제거 금지**, 갱신 시 기존 사진에 **append** 유지. 장소 전환 시 **이전 API 응답이 늦게 덮어쓰지 않도록** 요청 세대·장소 키 기준 유지.

### Supabase Edge Function 배포 (401 방지)

배포 시: `VITE_SUPABASE_URL`의 **subdomain = project ref** 확인 후, 예)

`npx supabase functions deploy <function_name> --project-ref <ref> --no-verify-jwt`

(프론트에서 무백 JWT 호출이면 **401** 방지를 위해 `--no-verify-jwt` **필요**한 케이스가 많음. 보안·역할이 바뀌면 다시 점검할 것.)

---

## 4. AI 협업 핵심 원칙

1. **기획 .md 남발 금지**: 사용자가 **명시적으로** 요청하지 않는 한 `plans/*.md`에 **새 “완료 보고서/대형 계획”**을 만들지 않는다. 필요한 건 **짧은 일지** 또는 **이 파일 5~6절** 갱신.
2. **구조 합의 → 직접 편집**: 가능하면 채팅에 **수백 줄 코드 덤프** 대신, 워크스페이스 **파일을 수정**한다.
3. **UI 임의 변경 금지**: 기존 레이아웃/반응형/모바일 동작을 **승인 없이** 뜯지 않는다. 한국어 문단은 `break-keep` 등 **기존** 규칙 유지.
4. **사용자 확인**: 수정 후 “완료” **단정** 직전, 사용자 **테스트·승인**이 없으면 “제안/대기”로 둔다. 단위 작업 후 **git commit**은 **제안**한다.
5. **고유명사**: `한글명('English name')` 형식 유지.
6. **툴킷·갤러리·제휴**(스마트 링크, 비자/페리, 링크 정합 등) **도메인 로직**은 3절, 해당 **소스·Edge Function**을 **우선**한다.
7. **Git 커밋 메시지**: **한글**로 작성한다(제목·본문 모두). Conventional prefix(`feat:`, `fix:` 등)는 선택.

---

## 5. 최근 스냅샷 (2026-04 기준, 상세는 아카이브)

- **2026-05-19 클룩 렌터카 배너 검색어(완료)**: 배너 URL 검색어 **여행지명 우선**(`resolveKlookRentalBannerSearchLabel`). 링크 버튼·여정 렌터카는 **렌터카 홈**. 예외 필드·운영: [`klook-rental-search-data.md`](plans/klook-rental-search-data.md)(공항·여행지 DB 후 `klookRentalSearchLabel` 추가).
- **2026-05-19 플래너 Trip.com 배너·모바일 도착지(`ce2ee7a`, 배포)**: 배너 iframe 직접 조작 · `partnerNavigation` · `omitLocaleBundle` 제거. **데스크 도착지 OK · 모바일 자동입력 미해결**. [`2026-05-19-project-log.md`](plans/2026-05-19-project-log.md) §플래너 Trip.com 배너 상호작용.
- **2026-05-19 플래너 Trip.com 항공 링크·CTA(완료·문서 구버전 주의)**: `/flights/` 직링크·`FlightSearchCta` 등 — 당일 후속으로 배너 오버레이·링크 정책 변경됨. 상세는 위 § 및 [`2026-05-19-project-log.md`](plans/2026-05-19-project-log.md).
- **2026-05-19 플래너 Trip.com·모바일 UX(완료)**: 모바일 배너 **S17158794** 320×480 · `useTripcomPlannerBannerDimensions` · FAB·핀치줌 등. 도착지 자동입력은 별도 § 미완. [`2026-05-19-project-log.md`](plans/2026-05-19-project-log.md).
- **2026-05-21 세션 D 정체성(완료·배포 대기)**: alias 바티칸→rome·사뭇→bangkok·아오시마→kumamoto · 승격 **havana**·**malta** · reconcile duplicateSlug **0** · delete-only 7 · Gate **none:0** · unmapped **11**(blocklist) · **266** 여행지 — [`2026-05-19-project-log.md`](plans/2026-05-19-project-log.md) §세션 D.
- **2026-05-21 Handoff JSON sync(배포 `235fb1d`)**: placeIds **335** · 그린란드·포클랜드·바하마 multi · Gate **none:0** · unmapped **21** → 세션 D 선행.
- **2026-05-21 세션 C 배포 스모크(완료, `83118cf`)**: gateo.kr 퀸스타운 **ZQN**·함부르크 **HAM**(배너 FRA 없음)·퍼스 **PER**·그린란드 **CPH·GOH** · explore `퀸스타운`/`Queenstown`·`함부르크`/`Hamburg` 자동완성 · `audit:airports none:0` · P0 unmapped 0 · unmapped **21**.
- **2026-05-21 세션 C placeIds_only 승격(완료)**: hamburg·cocos-islands·greenland·queenstown 등 **13 slug**(372–384) · **264** 여행지 · `audit:airports none:0` · unmapped **36→21**.
- **2026-05-21 10b8823 배포 스모크(완료)**: gateo.kr el-calafate **FTE**·patagonia 연관 칩(FTE)·explore `엘칼라파테`/`El Calafate` · P0 unmapped 0.
- **2026-05-21 플래너·배너·공항 정합(완료)**: 여정 vs 배너 어긋남 수정(SPU·NGS·MDZ·CUZ 허브, hvar·nagasaki·inca-trail·aconcagua·borneo curated). **`searchHintIatas`** — 검색·렌터카·권역 칩은 연동 공항만; **환승(EZE·LIM·KUL)** 은 `bannerNote`. **`resolveBannerPeerAlternateAirports`** — 「다른 도착 후보」는 동급 관문 2+일 때만. [`plans/2026-05-21-project-log.md`](plans/2026-05-21-project-log.md).
- **2026-05-21 코토르·generate 보호(완료)**: **kotor** TIV·TGD curated(DBV 오탐 보정). **`generate:airports`** — JSON만 있는 `curated-override` 보존·overrides.mjs 우선·`searchHintIatas` 병합·drift 경고(6절 체크리스트).
- **2026-05-21 세션 G el-calafate(완료)**: slug **el-calafate**(371)·FTE · **patagonia-region** 4-slug · DB IATA patch 1건 · `resolveTravelSpotFromCoords`+`getPlaceUrlParam` slug 우선(loc URL 새로고침 「알 수 없는 지역」수정). [`2026-05-19-project-log.md`](plans/2026-05-19-project-log.md).
- **2026-05-19 연관 여행지 UI 1차(완료)**: Gate 통과 · `travelSpotClusters.json` · PlannerTab `RelatedTravelSpots` · D-3 허브·IATA 패치 7건. [`related-destinations-cross-nav-plan.md`](plans/related-destinations-cross-nav-plan.md).
- **2026-05-19 파타고니아 slug 분리(완료)**: `patagonia`(북부 BRC/EZE)·`ushuaia`(USH)·`torres-del-paine`(PUQ).
- **2026-05-19 여행지 정체성 Phase D-2(완료)**: bohol·yokohama·tsushima curated · DB IATA 7건 · **`skippedNoIata` 26→19** · `audit:airports none:0`. gateo.kr QA 킬리만자로·싱가포르·런던·제주 IATA 일치(3586fec).
- **2026-05-19 여행지 정체성 Phase D(완료)**: curated high · DB IATA 12건 · **`skippedNoIata` 37→26** · `audit:airports none:0`. [`2026-05-19-project-log.md`](plans/2026-05-19-project-log.md).
- **2026-05-19 여행지 정체성 Phase 2·3(완료)**: P1–P3 reconcile(295행) · Phase **2d** Edge canonical `place_id` · **3a** curated 승격(galapagos·iceland·phuket·reykjavik·patagonia 등) · DB IATA 패치 4건. **`duplicateSlug 0`** · **`geoMismatch 0`** · `audit:airports none:0`. Edge `update-place-toolkit` **재배포 필요**. [`2026-05-19-project-log.md`](plans/2026-05-19-project-log.md).
- **2026-05-18 여행지 정체성 Phase 1(완료)**: `mergeCanonicalTravelSpot`·`getPlaceStableKey` — 검색·핀·URL 복귀 시 SSOT slug 병합. [`destination-airport-identity-plan.md`](plans/destination-airport-identity-plan.md).
- **2026-05-18 여행지 정체성·공항 P0 세션3(완료)**: `toolkit:reconcile-place-id --apply --only=angkor-wat` — `앙코르와트` 삭제·canonical `앙코르 와트` 통합. P0 audit duplicateSlug 0. gateo.kr 앙코르·「시엠립」검색 플래너·Trip.com **SAI** QA. 브루나이 `place_toolkit` 행은 병합하지 않음(unmapped 1).
- **2026-05-18 여행지 정체성·공항 P0 세션1(완료)**: 별칭·`travelSpotAirports.json`·SAI 오버라이드. `audit:airports` `none: 0`.
- **2026-05-18 플래너 Trip.com 항공(완료·프로덕션 배포)**: Travelpayouts 화이트라벨·미사용 `HotelWidget` 제거. **`buildTripcomPlannerFlightUrl`** — `dAirportCode=ICN`, `aAirportCode`=`resolveRentalPickupBannerInfo` SSOT. **`TripcomFlightBannerWidget`**(ad S17104971) 툴킷 상단. gateo.kr 배포 완료(Vercel `main` `0d92bb5`). 상세 [`plans/2026-05-18-project-log.md`](plans/2026-05-18-project-log.md).
- **2026-05-14 홈 장소카드·AI 채팅 요약(완료)**: 써머리 카드에서 **지명 복사 제거**, 지명 영역은 **확장 카드 진입**과 동일 동작(`PlaceCardSummary`). **「AI에게 장소 묻기」** 모달 진입 시 빈 화면을 줄이기 위해 여행지 **짧은 AI 요약**을 상단에 표시하고, **`place_chat_intro`** 테이블(Supabase, `destination_key`+`summary`, RLS·GRANT)에 저장·재사용; 보조는 `placeChatIntro.js`(원격 우선, 로컬 폴백), 프롬프트는 `getPlaceChatIntroSystemPrompt()`. 상세 [`plans/2026-05-14-project-log.md`](plans/2026-05-14-project-log.md). *(후속 선택: 동일 요약을 채팅 system에 주입해 대화 컨텍스트로 활용.)*
- **2026-05-17 플래너 툴킷 로드·도착 공항 보정(완료)**: [`toolkitPlaceIdResolve.js`](src/utils/toolkitPlaceIdResolve.js) — `place_toolkit` 조회 시 `name`·`name_en`·`slug`·별칭 **다중 `place_id`**, `essential_guide` **실내용·지리 일치** 검증(빈 `{}`/타 여행지 IATA 제외). [`usePlannerData`](src/components/PlaceCard/hooks/usePlannerData.js)는 `location` 객체 전달·불일치 행 무시. **배너**: 허브 **TIM·CHQ·SCQ** 등 추가, **경유 허브(MAD·DJJ·ATH)** 오탐 필터·`medium` 오버라이드보다 live 툴킷 우선. 검수: 카르스텐츠 **TIM**(DJJ X), 크레타 **HER+CHQ**, 산티아고 **SCQ+MAD**. `update-place-toolkit` 저장 전 `primary_arrival_airports_iata`·좌표 거리 검증(범용). 상세 [`plans/2026-05-17-project-log.md`](plans/2026-05-17-project-log.md).
- **2026-05-17 여행지–도착 공항·툴킷 동기화(완료)**: SSOT [`travelSpotAirports.json`](src/pages/Home/data/travelSpotAirports.json) — **`spots`**(공식 slug 244) + **`placeIds`**(DB `place_id`·사용자 지명·별칭 키, sync 시 ~286키). 런타임 우선순위: **수동 오버라이드 > live `essential_guide` > toolkit-sync JSON > runtime-infer**. `npm run sync:airports-from-toolkit`(DB **읽기만**, `.env.local` + `SUPABASE_SERVICE_ROLE_KEY`). 별칭 [`travel-spot-place-id-aliases.mjs`](scripts/data/travel-spot-place-id-aliases.mjs)·[`travel-spot-place-resolve.mjs`](scripts/lib/travel-spot-place-resolve.mjs). 피피 섬 등 좌표 오탐(KBV) → 툴킷 HKT 정합.
- **2026-05-17 플래너 도착 공항 배너·매칭(완료)**: `RentalPickupBanner`로 「렌터카 · 픽업 · 항공권 기준」**배너 재활성화**. `PlannerTab` 배치는 **제목 → Trip.com 항공 제휴 배너 → 공항 배너 → 본문**(2026-05-18 패키지 배너 교체). 타임라인 **역순**·**경유 단계 제외**·목적지 근접 필터; **미등록 IATA는 무시** — 허브·JSON·별칭 동기화 필수. 상세 [`plans/2026-05-14-project-log.md`](plans/2026-05-14-project-log.md).
- **2026-05-14 플래너 카피·항공 UX(완료)**: `getFlightDestinationSearchHint` 다중 공항 안내 문구 축약; `ToolkitCard` 공항 이동 카드 하단 부연 제거·**공항 픽업 검색** 링크에 `subtext`로 항공편명 안내; `PlannerTab` 렌터카·항공 기준 배너 문구 간결화; 항공 CTA는 **Trip.com 새 탭**(2026-05-18, 구 Travelpayouts 화이트라벨); `JourneyTimeline`에서 AI 제목의 **(IATA)** 구간마다 **앞 도착 지명만**·**코드만(ASR 등)** 각각 복사(정식 공항명으로 치환하지 않음). 상세 [`plans/2026-05-14-project-log.md`](plans/2026-05-14-project-log.md) §플래너 툴킷·여정 UX 후속.
- **2026-05-14 플래너 공항·클룩·로포텐(완료)**: `rentalAirportHubs`에 **PDL·FAE·BOO·EVE·LKN·SVJ**, DEN `den` 오탐 제거. `rentalAirportMatch`에 저장값·좌표 불일치 보정, IATA-only 정식명 보강, **`getFlightDestinationSearchHint`** IATA 우선, **`getRentalCarHomeSearchSubtext`**, 다중공항 **`bannerNote`**, 로포텐 전용 문구. **`affiliate`**: **`getKlookRentalHomeUrl`**(`KLOOK_RENTAL_HOME_AD_ID` 1277252), 검색 폴백은 **`KLOOK_DEFAULT_AD_ID`(1256120)**. 공항 이동 **렌터카 홈** 링크 + 배너는 검색 URL; **KlookCar·Tour 배너** iframe `pointer-events-none`으로 제휴 리다이렉트 클릭 고정. **`PlannerTab`** 에 로포텐 `bannerNote` 블록. 상세 [`plans/2026-05-14-project-log.md`](plans/2026-05-14-project-log.md) §플래너 공항·클룩 제휴·로포텐.
- **2026-05-10 플래너·제휴 UI(완료)**: 필수 앱 구글 검색에 ` APP` 접미사(`CopyableText`). **Klook** 명소 투어/공항 렌터카 배너는 뷰포트별 크기(`useKlookPlannerBannerDimensions`: 투어 모바일 300×250·데스크톱 468×60, 렌터카 모바일 250×250) + **`data-adid` 분기**(투어 1273972/1272015, 렌터카 1273974/1265731) + `klookBannerLayout` 스케일 잘림 수정. 명소 **Klook 레스토랑**은 `locationRules.js` 인도네시아 전체 매칭 제거·자바권 키워드 보강. 유심 **Airalo/Holafly** `src/assets` 번들·그룹 래퍼·`ToolkitCard` **`className`**. **플래너** 긴 스크롤 시 **맨 위로** FAB. **교통** Bounce는 `bounce_278x90.png` / `bounce.png` + `<picture>`(`ToolkitCard`). 상세 [`plans/2026-05-10-project-log.md`](plans/2026-05-10-project-log.md).
- **2026-05-08 플래너 제휴 위젯 운영 정리(완료)**: `map_poi` 투어 노출을 위젯 중심으로 재정렬(클룩 배너 우선, GYG 폴백 지역은 GYG city 위젯). GYG 매핑을 카나리아 제도(416)·코르시카(1534)·피오르드랜드(32367)·잔지바르(871)까지 확장. 클룩 투어 검색어를 `여행지명 + 투어`로 통일하고 텍스트 투어 링크는 제거. 다이닝은 `Klook 전략 지역 -> 대안 플랫폼 -> 범용 대안` 분기로 재구성하고, 지역별 규칙을 `src/components/PlaceCard/tabs/planner/locationRules.js` 단일 파일로 통합.
- **2026-05-07 감정 기반 검색바 정체성 강화(완료)**: 홈/탐색 검색 UX를 여행지 키워드 중심에서 **느낌·감정 입력 중심**으로 전환. 검색 안내 문구를 "지금 기분/느낌" 톤으로 재작성하고, AI 검색 프롬프트를 단순 오타 교정기에서 **감정-여행 연결 큐레이터**로 승격. 장소카드 설명에 "왜 이 여행지를 추천했는지" 1문장 이유를 포함. 동시에 허구 지명 방지를 위해 **지명-좌표 교차검증(재지오코딩+거리검증)**을 적용하고, 캐시는 스키마 변경 없이 `search_dictionary.location_data` JSON 내부에서 `intent_type: typo|mood`, `variants`, `served_count`, `last_served_at`를 운용해 감정 검색 결과의 **무작위성+안정성**을 함께 확보.
- **2026-05-04 장소라우팅·복원**: 검색·지오코딩 장소의 영문 `slug`가 유명 도시(slug)와 겹치면 `/place/잘못된-slug`로 열리던 문제 → **`getPlaceUrlParam()`** (`formatUrlName.js`)로 `search-…`·`loc-…` **id를 URL에 우선**. **`placeLocationCache`** (`sessionStorage`, `placeLocationCache.js`): `/p/블로그` 등으로 `Home`이 언마운트된 뒤 뒤로가기 시 좌표 URL만으로는 표시명이 없어지던 것을 **같은 키로 캐시 복구**. `ReviewsTab`: `search-`/`loc-`일 때 리뷰 키는 **`id` 우선**. `PlaceCardExpanded`: 탭 이동 시 **`pathname` slug 우선**해 잘못된 `location.slug`로 재네비 방지.
- **2026-05-04 place_stats (계획만)**: `place_id`를 한글 표시명 단일 축으로 쓰는 한계·갤러리/RPC 정렬 → **stable key + 메타 컬럼** 마이그레이션 실행안은 [`plans/2026-05-04-project-log.md`](plans/2026-05-04-project-log.md) Phase A–D.
- **2026-05-21 slug-first (완료)**: `place_alias` 1408 · migrate apply(rekey 952) · `place_id`=slug · gateo.kr angkor-wat/시엠립 단일 툴킷(SAI) · duplicateSlug 0 — [`2026-05-21-slug-first-migration-handoff.md`](plans/2026-05-21-slug-first-migration-handoff.md).
- **2026-05-04 LogBook(블로그)**: 공개 피드·`/p/:id`·명소 **관련 여행기**에 작성자 표시 — `profiles.display_name` 배치 조회(`reportAuthor.js`), 없으면 `user_id` UUID 앞 8자. **필명**은 `usePenName` → `profiles` upsert(`id`, `display_name`, `updated_at`). **`PenNameContext`**로 `DailyLayout` 하위 사이드바·`/blog/write`가 **동일 필명 상태** 공유. Supabase `profiles` **INSERT RLS**(`auth.uid() = id`) 없으면 upsert(신규 행) 403 — 정책은 DB에서 유지.
- **2026-05-04 탐색·제휴**: `/explore` 큐레이션 첫 섹션 맨 앞에 **트립닷컴** 호텔 세일 카드(`TRIPCOM_EXPLORE_LEADING_CARD`, 제휴 딥링크·문구). 배경 이미지는 Unsplash 세로 호텔 톤(URL, `SearchDiscovery/constants.js`).
- **2026-05-04 장소카드 갤러리**: `usePlaceGallery`에서 여행지 이동 시 **이전 장소 사진이 남던 현상** 수정 — 영문 검색어만으로 중복 요청을 스킵하던 로직을 **장소 키(slug/id/name)+쿼리** 기준으로 바꾸고, 비동기 완료 시 **요청 세대(runId)** 로 오래된 응답 적용을 차단. 세션 캐시 키도 장소별로 분리.
- **2026-05-04 플래너 호텔**: 장소카드 플래너 **숙소 버튼 기본값은 마이리얼트립** 동적 검색; 트립닷컴은 `affiliate.js`의 **`PLANNER_TRIPCOM_HOTEL_OVERRIDES`**에 여행지별 **전체 제휴 URL**을 넣은 경우에만 예외 연결(`getTripcomHotelOverrideUrlForLocation`). 상세는 [`plans/2026-05-04-project-log.md`](plans/2026-05-04-project-log.md).
- **SEO/검색**: JSON-LD(TouristAttraction), Helmet, 네이버/사이트맵·RSS, www·봇 대응
- **제휴·콤플라이언스**: TripLink(패키지·쿼레이션), Direct Ferries, “제휴광고/중개” 문구 정리
- **데이터**: **264** 여행지, `travelSpots-list.json` + 추출 스크립트
- **보안**: **Gemini 등 API 키** 브라우저 노출 제거, **Edge 프록시**·허용 모델, Mapbox 도메인 제한
- **2026-04-26 유지보수(요약)**: `.ai-context`·`plans` 아카이브, `useClick`/`SearchDiscovery`·`index.html`, 미사용 레이아웃·백업 js, `scripts/outputs/` + **ESLint** `npm run lint` **0 errors / 0 warnings** (동일 세션·`exhaustive-deps` 정리: `useCallback`/`useLocation`/`eslint-disable-next-line` 근거, UX 미변경), `formatUrlName` / `isMobileDevice`·`device.js` 분리
- **Home AI 채팅·`saved_trips`**: 모달만 열 때는 DB insert 없음 → **첫 유저 메시지**에서만 trip 생성(`chatDraft`, `createTripOnFirstUserMessage`). 채팅 사이드바·지구본 말풍선은 `tripHasPersistedDialogue`(`src/pages/Home/lib/tripChatUtils.js`)로 **저장된 대화가 있는 항목만** 표시.
- **2026-04-28 제휴 링크 정비(요약)**: 플래너 `connectivity` 카드에서 Klook 제거 후 Airalo/Holafly 2버튼으로 전환, 클룩 렌터카 배너(728x90) `transport`·`airport_transfer`에 통일 적용, `affiliate.js`에 클룩 딥링크 공통 유틸/도시별 렌터카 매핑(홍콩·마카오·타이베이·이란·도쿄·오사카·교토·북해도/홋카이도·규슈) 추가, `USE_KLOOK_LOCALE_PATH` 토글 도입(현재 `true`) 및 Travelpayouts linkswitcher 개입 영향 확인.
- **2026-04-28 플래너 배너 UI 보정(추가)**: 유심 파트에 Airalo/Holafly 이미지 배너를 도입(각 `public/airalo-page2.png`, `public/holafly-asia-banner.png`)하고 한국어 카피/모바일 `break-keep` 가독성을 조정. 최종적으로 렌터카 배너는 중복 제거 후 `airport_transfer`에만 유지.
- **2026-05-05 Home 글로브 전환 준비(진행 예정)**: 홈 글로브는 현재 `src/pages/Home/components/HomeGlobe.jsx`의 `react-globe.gl` 기반. `Home`은 `globeRef`로 `flyToAndPin`/`resumeRotation`/`pauseRotation`/`resetPins` 계약에 의존. Mapbox(`mapbox-gl`/`react-map-gl`)는 이미 의존성 및 `PlaceMiniMap`에서 사용 중이며 토큰은 `VITE_MAPBOX_TOKEN` 사용. 전환 전략은 **기존(legacy) 유지 + Mapbox 신규 엔진 추가 + 어댑터 스위치 + 실패시 자동 폴백**으로 진행.
- **2026-05-05 Home Mapbox 글로브 1차 적용(완료)**: `HomeGlobeAdapter`(엔진 스위치), `HomeGlobeMapbox`(Mapbox projection globe)를 추가하고 홈 진입 글로브를 어댑터로 연결. 기본은 Mapbox 우선, 토큰 누락/런타임 오류 시 legacy 자동 폴백. `deep` 테마는 위성 기반(`satellite-streets-v12`)으로 유지하며, 물 레이어 블루 보정/테마별 fog 구성으로 시각 톤을 조정.
- **2026-05-05 Home Mapbox 글로브 2차 튜닝(완료)**: 첫 입력 전 드래그/줌이 먹지 않던 상호작용 이슈를 포인터-우선 제어로 안정화. 지도 텍스트 정책은 `PLACE_LABEL_MIN_ZOOM` 이상에서만 지명 표시, 도로/POI/행정경계는 레이어 가시성과 `basemap` config를 함께 적용해 재노출을 차단. 최종 모드는 `deep` 기본(위성 계열), `bright` 표준, `neon` 위성 계열이며 `deep`에는 약한 푸른 윤곽 대기를 적용.
- **2026-05-05 Home Mapbox 글로브 3차 UX(완료)**: 장소 선택 시 과도한 카메라 이동(`flyTo`)을 중지해 확대 탐색 컨텍스트를 유지. 글로브 우측에 빠른 액션(공유/현재 위치/우주 복귀) 버튼을 추가하고, 티커 클릭영역이 버튼을 가리지 않도록 `pointer-events`를 분리. 데스크톱 상단 컨트롤 재배치(테마·Zen·핀·휴지통 묶음, 우측 액션 상단 정렬) 및 아이콘 톤(공유 iOS 스타일, 위치 화살표, 지구본 강조) 조정.
- **2026-05-05 PlaceCard 반응형 정책 조정(완료)**: 태블릿에서 PlaceCard가 데스크톱 2단(`md`)으로 보이던 구성을 모바일형으로 통일하기 위해 2단 전환 브레이크포인트를 `lg` 이상으로 상향. 요약 카드(`PlaceCardSummary`) 우측 고정도 `lg`부터만 적용.
- **2026-05-06 Home Mapbox 마커 최적화(완료)**: 여행지 노출을 `tier` 기반 단계 + 근접 병합 + 2차 충돌 완화 구조로 재구성. 운영 규칙은 **초기/중간 줌에서는 선택 카테고리 유지**, `HIGH_ZOOM_FULL_REVEAL(3.0)` 이상에서만 전체 여행지(`allTravelSpots`) 개방으로 정리. 기본 지명은 `PLACE_LABEL_MIN_ZOOM = 4.0`으로 상향해 지명 혼잡 전 마커 탐색 구간을 확보. 향후 여행지 추가로 겹침이 늘면 `TIER_STAGE_ZOOM_LEVELS`, `HIGH_ZOOM_FULL_REVEAL`, `getMajorMergeThreshold()`, `getMarkerCollisionThreshold()` 4개 임계값을 우선 튜닝.
- **2026-05-06 PlaceCard/GYG 운영 반영(완료)**: 요약/확장 장소 카드 헤더에 영문명(`name_en`) 보조 표기를 추가. 플래너 `map_poi`에 `GetYourGuideCityWidget`(partner `LRKVVU4`)을 도입하고 `index.html` head에 GYG analytics 스크립트를 삽입. 현재 GYG 매핑/전용 노출 여행지: 에베레스트(2008), 코스타리카(168995), 갈라파고스(204933), 파타고니아(2794). 해당 여행지는 `map_poi` 기타 링크를 숨기고 위젯만 노출.
- **2026-05-06 홈 복귀 지구본 축소 버그 수정(완료)**: `/place -> /explore -> /` 복귀 시 `pauseRender` 해제 타이밍에 Mapbox 캔버스가 축소 상태로 남는 현상을 `HomeGlobeMapbox`의 `requestAnimationFrame` 기반 이중 `map.resize()` 보정으로 해결.
- **2026-05-07 Home Mapbox 클릭/복귀 UX 정리(완료)**: 지도 클릭 이벤트를 **마커 > 라벨 > 빈 지도(역지오코딩)** 순으로 분리하고, 라벨 클릭은 카드 한글 표기 + URL 영문 slug를 함께 만족하도록 조정. 직접 지도 탐색 클릭은 `focus:false`로 카메라 이동을 막고, 검색/카드/탐색 유입은 `focus:true`로 홈 복귀 시 재포커스를 허용. `pauseRender` 중 누락되던 포커스는 `pendingFocus`로 지연 적용하고, `/place`·`/explore`에서는 지구본 렌더를 확실히 정지. `onIdle` 레이어 재스캔 제거로 뒤로가기/연관키워드 전환 체감 지연 완화. 장소카드 헤더에 홈(지구본) 바로가기 버튼 추가.
- **2026-05-08 Home 글로브 모바일 안정화(진행)**: 모바일에서 마커 탭 이후 빈 지구본 탭이 막히는 회귀를 `HomeGlobeMapbox`/`HomeGlobe`의 hover click-guard 정리로 완화. 현재 위치 버튼은 Mapbox에서 `onGlobeClick` 경로를 재사용해 핀 생성 일관성을 맞춤. 로컬 모바일 Mapbox는 토큰 URL 제한(403, 이후 429+CORS 노출) 영향이 확인되어 DEV 모바일 기본 엔진을 legacy로 두고 QA 진행, 배포 경로는 Mapbox 유지.
- **2026-05-09 탐색·모바일·LAN·지구본(완료)**: `SearchDiscoveryModal` 검색 기록·fixed popover·`visualViewport`·비차단 백드롭 제거 등은 [`plans/2026-05-09-project-log.md`](plans/2026-05-09-project-log.md). 추가로 탐색 오버레이 **갈색 톤**, 장소카드 **모바일 갤러리**에 `desc`/`originalQuery`/사진 캡션 노출, `vite` **HMR `wss`+`strictPort`**로 LAN HTTPS 보조. 로컬 모바일: **PC와 동일 서브넷**·Win 방화벽 TCP 5173·자체 서명 통과; 막히면 **푸시 후 배포 URL QA**. `HomeGlobe`/`HomeGlobeMapbox`의 `pauseRender`는 **`display:none` → `invisible`**로 전환 시 깜빡임 완화.
- **2026-05-09 장소카드 모바일·플래너(후속)**: 리뷰 탭 보조 네비 복구·헤더 스크롤 동행·리뷰용 라이트 탭 스타일. 플래너는 DB만 `refetchPlannerFromDb`, 탭 진입 시 **AI 자동 실행 제거**(버튼·빈 상태 「AI 툴킷 실행하기」만), 앱 전송 CTA 제거. 모바일 갤러리 설명에서 **#키워드** 제거. 일지 동일 파일 [`plans/2026-05-09-project-log.md`](plans/2026-05-09-project-log.md) §장소카드 모바일·플래너·갤러리.

장문 타임라인·커밋 단위 히스토리는 [legacy 아카이브 `plans/archive/legacy-2026-04-root/`](plans/archive/legacy-2026-04-root/)와 **git**에 둔다. 이 파일 5절에 **이력을 쌓지 말 것**.

---

## 6. 남은 과제 (다음에 묶어서)

### 여행지–도착 공항 매핑

**운영 가이드**: [`plans/travel-spots-management.md`](plans/travel-spots-management.md) · 클룩 렌터카 배너 검색어: [`plans/klook-rental-search-data.md`](plans/klook-rental-search-data.md)

| 파일 | 역할 |
|------|------|
| [`travelSpotAirports.json`](src/pages/Home/data/travelSpotAirports.json) | **`spots`**: 공식 slug · **`placeIds`**: DB `place_id`/사용자 지명(별칭 키 포함) |
| [`rentalAirportHubs.js`](src/utils/rentalAirportHubs.js) | IATA 한글 공식명·좌표·`aliases` |
| [`rentalAirportMatch.js`](src/utils/rentalAirportMatch.js) | 배너·제휴 링크 해석(우선순위 아래) |
| [`toolkitPlaceIdResolve.js`](src/utils/toolkitPlaceIdResolve.js) | 플래너 DB `place_id` 후보·`essential_guide` 유효성 |
| [`travel-spot-airport-overrides.mjs`](scripts/data/travel-spot-airport-overrides.mjs) | slug 수동 검수(툴킷·추론보다 우선) |
| [`travel-spot-place-id-aliases.mjs`](scripts/data/travel-spot-place-id-aliases.mjs) | `place_id` → slug 별칭(에베레스트→everest-base-camp 등) |

**런타임 우선순위**: 수동 오버라이드 → **live `essential_guide`** → `toolkit-sync`(JSON) → `runtime-infer` → multi-rule·좌표.

**툴킷이 쌓일 때마다** (DB 수정 없음):

```powershell
cd c:\dev\days
npm run sync:airports-from-toolkit
```

공식 여행지 `spots` 일괄 재생성: `npm run generate:airports` (**`placeIds`는 유지**). 감사: `audit:airports` · `enrich:airports`(리포트).

**`generate:airports` 우선순위**(스크립트·런타임과 구분):

1. [`travel-spot-airport-overrides.mjs`](scripts/data/travel-spot-airport-overrides.mjs) — **명시 SSOT**, 있으면 JSON보다 우선  
2. JSON `spots` 중 `source: curated-override` — **overrides에 slug 없을 때만** 보존  
3. JSON `spots` 중 `source: toolkit-sync`  
4. runtime-infer / multi-rule / 좌표  

- overrides에 `searchHintIatas`가 없으면 JSON 검수값을 **병합 유지**.  
- overrides가 기존 curated `primaryIatas`와 다르면 **콘솔 경고**(의도 수정 vs 데이터 불일치 확인).  
- **`travelSpotAirports.json`의 `spots`만 직접 수정하지 말 것** — 검수는 overrides에 넣은 뒤 generate. (`placeIds`·툴킷 반영은 `sync`.)

| 기대 | |
|------|--|
| sync | `slug 동기화` + `placeId만 동기화`(비공식 지명); 리포트 `scripts/outputs/travel-spot-airports-toolkit-sync.json` |
| generate | `Mapped: N / N`, `missing: 0`; `curatedPreserved`·drift **Warning** 확인 |
| audit | `none: 0`; `inferNearestMismatch`·`skippedNoSpot` 검수 |

### 여행지–페리·크루즈 매핑

**운영 가이드**: [`plans/travel-spots-management.md`](plans/travel-spots-management.md) **2절·4절**

| 파일 | 역할 |
|------|------|
| [`travelSpotFerries.json`](src/pages/Home/data/travelSpotFerries.json) | slug → `tier`, `routes`, `fallbacks`, `dfRecommendations` |
| [`travel-spot-ferry-overrides.mjs`](scripts/data/travel-spot-ferry-overrides.mjs) | 검증된 노선·운항사·12Go URL (SSOT) |
| [`affiliate.js`](src/utils/affiliate.js) | 12Go 제휴 `get12GoAffiliateUrl` · Klook/Trip 등 |
| [`ferryBookingMatch.js`](src/utils/ferryBookingMatch.js) | 플래너 페리 카드 노출·예약 URL resolve |
| [`TwelveGoSearchWidget.jsx`](src/components/PlaceCard/tabs/planner/components/TwelveGoSearchWidget.jsx) | 12Go 제휴 클릭 배너 (노선별) |

**여행지 추가 시 페리 필수 단계**: tier 판단(`required`/`common`/`cruise_only`/`none`) → ferry overrides → `npm run generate:ferries` → `npm run audit:ferries`(gap 0). **`travelSpotFerries.json` 직접 수정 금지.**

| 기대 | |
|------|--|
| generate:ferries | 신규 slug가 JSON에 반영 |
| audit:ferries | `gapCount: 0` |

### 여행지 추가·도착 공항·페리 체크리스트

1. **IATA** — [`rentalAirportHubs.js`](src/utils/rentalAirportHubs.js)에 한글 공식명·좌표·`aliases` 등록(없으면 배너·링크 무시).  
2. **공항 검수** — [`travel-spot-airport-overrides.mjs`](scripts/data/travel-spot-airport-overrides.mjs)에 `primaryIatas`·`preferredLinkIata`·다공항이면 `kind: multi`·`searchHintIatas`(연동 공항만)·`bannerNote`(환승·**버스·페리**).  
3. **공항 반영** — `npm run generate:airports` → `git diff travelSpotAirports.json`에서 **해당 slug만** 변경됐는지 확인.  
4. **페리 tier** — `required`/`common`/`cruise_only`/`none` 판단([`travel-spots-management.md` 2절 5a](plans/travel-spots-management.md)).  
5. **페리 검수** — `none`이 아니면 [`travel-spot-ferry-overrides.mjs`](scripts/data/travel-spot-ferry-overrides.mjs)에 `routes[].bookings`(운항사·DF·12Go) 또는 `fallbacks`.  
6. **페리 반영** — `npm run generate:ferries` → `npm run audit:ferries` (**gap 0**).  
7. **툴킷** — DB `essential_guide`와 어긋나면 overrides가 SSOT; 툴킷만 갱신할 때는 `npm run sync:airports-from-toolkit`(override slug는 건너뜀).  
8. **QA** — gateo.kr 플래너 배너·Trip.com `aAirportCode`·**페리 카드·12Go 배너·운항사 버튼**·(크루즈) 타임라인 Trip.com.

`travelSpots`에 **없는** 지명(코코스·핏케언 등)은 **`placeIds`만** — slug 별칭으로 억지 연결하지 말 것. `place_id` 별칭은 [`travel-spot-place-id-aliases.mjs`](scripts/data/travel-spot-place-id-aliases.mjs)에 추가.

**후속**: 프론트·JSON **배포** 후 gateo.kr 스모크(havana·malta·바티칸→rome) · `unmapped` **11**(blocklist·어센션 섬) · (선택) 연관 칩 · Trip.com 모바일(미완).

### 연관 여행지·세부 slug

**계획서**: [`plans/related-destinations-cross-nav-plan.md`](plans/related-destinations-cross-nav-plan.md)

- **1차 완료**: 파타고니아·아이슬란드·보르네오 클러스터 + PlannerTab `RelatedTravelSpots` · **el-calafate**(2026-05-21).
- **다음**: 배포 후 gateo.kr QA(havana HAV·malta MLA·rome/바티칸) · (선택) 연관 칩 · Trip.com 모바일.

---

- **`place_stats` 정체성 정렬** (우선 실행안: [`plans/2026-05-04-project-log.md`](plans/2026-05-04-project-log.md)): `place_id` = stable key 통일, `name_ko`/`lat`/`lng`/RPC·갤러리·`recordInteraction`·티커 매칭, 마이그레이션·RLS.
- Direct Ferries **노선 데이터** 축(일본/북유럽/지중해 등) — *선택/Phase2* · **페리 overrides 점진 보강**(12Go URL·`twelveGoBannerLabel` 검수·신규 섬)
- **끊긴·구식** 제휴/OTA **링크 정리** (툴킷/플래너)
- **Home Mapbox 포커스 마무리 튜닝(후속)**: `pendingFocus`를 place id 기준 마지막 요청 1건만 유지하는 디바운스 및 라벨 노출 줌에서 UX 힌트(토스트) 추가 여부 검토.
- **지오코딩 호출 서버 이전(근본 해결, 우선순위 상향)**: 브라우저에서 Nominatim/외부 지오코딩을 직접 호출하지 않고, 서버(Edge Function/API Route) 프록시로 통합한다.  
  1) `reverse`/`search` 엔드포인트를 분리해 클라이언트는 내부 API만 호출,  
  2) 서버에서 레이트리밋·백오프·좌표/쿼리 캐시·공급자 장애 폴백을 공통 처리,  
  3) 클라이언트 `geocoding.js`는 fetch 대상만 내부 API로 단순화,  
  4) 로컬/배포에서 동일 동작 보장(브라우저 CORS/429 노출 차단) 및 공급자 교체 여지 확보.
- (백로그) 프롬프트·DB(공항명 등), A/B, 이미지 압축, 개인화. `emrld`(`index.html`) — Travel Payouts 소유자 확인·**유지**(삭제 시 대시보드 로그인 불가로 보고됨)

(과거 `Phase 9-2` 100곳 등은 **취소/완료/아카이브**로 정리됨. 새 목표는 위 3가지 + 사용자 요청이 우선.)

---

## 7. 문서·로그 (유지 루틴)

- **이 파일(`.ai-context.md`)** 은 **짧게** (대략 **~250줄 이내**). 히스토리는 `plans/…-project-log` **또는** [legacy-2026-04-root](plans/archive/legacy-2026-04-root).
- **일지**: `plans/YYYY-MM-DD-project-log.md` (루트) — 50KB/1000줄 넘기면 **새 파일**로 분기; **첫부에** 직전 일지 링크.
- **Git 커밋**: 메시지는 **한글**로 작성(4절 7항). 사용자가 commit을 요청한 세션 종료 시 `git status` → 관련 파일만 stage → 한글 메시지로 commit.
- [전체 2026-04 루트 문서 이동](plans/README.md) (2026-04-26)

---

## 8. 세션 종료 시 (Cursor/공용)

1. `git status` / 필요 시 **commit** (**한글** 메시지)
2. `plans/YYYY-MM-DD-project-log.md` **오늘 2~5줄** (없으면 **새** 파일)
3. **필요할 때만** [`.ai-context` **날짜·5~6절**] 3줄 수준 갱신

(과거: Claude/Roo 전용 `update_todo_list` / `attempt_completion` / Architect 모드 등 **도구·제품별 문구**는 **삭제** — 이 저장소는 Cursor 등 **범용** 기준으로 통일)
