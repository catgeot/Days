# gateo.kr 프로젝트 컨텍스트 (AI Context)

## 1. 프로젝트 개요
- **서비스명**: gateo.kr
- **설명**: React 기반의 모던 웹 애플리케이션으로, Supabase를 백엔드로 사용하며 3D 시각화 및 AI 기능을 통합한 서비스입니다.

## 2. 주요 기술 스택
- **Frontend**: React 19.2.0, Vite, React Router DOM
- **Styling**: Tailwind CSS, Lucide React
- **Backend & BaaS**: Supabase (`@supabase/supabase-js`)
- **3D & Data Visualization**: `react-globe.gl`, `three`, `@react-three/fiber`, `@react-three/drei`
- **AI Integration**: `openai`, `@google/generative-ai`
  - **사용 가능한 Gemini 모델 목록 (2026.03.12 기준)**:
    - `gemini-3.1-pro` (텍스트 출력 모델)
    - `gemini-2.5-pro` (텍스트 출력 모델)
    - `gemini-2.5-flash` (텍스트 출력 모델)
    - `gemini-2.5-flash-lite` (텍스트 출력 모델)
    - `gemini-3.1-flash-lite` (텍스트 출력 모델)
    - 기타 모델들은 API 콘솔 참조
- **State Management**: React Context API
- **Deployment**: Vercel

## 3. 폴더 구조 및 아키텍처
- `src/components/`: 재사용 가능한 UI 컴포넌트
- `src/pages/`: 라우팅되는 페이지 단위 컴포넌트
- `src/context/`: Context API를 활용한 전역 상태 관리 로직
- `src/shared/`: 공통 유틸리티, 커스텀 훅, 상수 등
- `supabase/`: Supabase 관련 설정, 데이터베이스 스키마 및 마이그레이션

## 4. 🤖 AI 어시스턴트 핵심 행동 원칙 (Core Rules)
사용자의 요청에 따라 코드를 수정하거나 기능을 추가할 때 다음 프로세스를 **엄격하게 준수**합니다:
1. **논리적 구조 제안**: 수정 및 구현하려는 코드의 논리적 구조와 방향성을 먼저 설명합니다.
2. **사용자 승인**: 제안한 구조에 대해 사용자의 승인을 대기합니다.
3. **직접 파일 수정(Edit Tool 사용)**: 승인이 완료되면 채팅창에 전체 코드를 길게 출력하며 토큰을 낭비하는 대신, AI가 에디터의 파일을 직접 수정(Edit)하여 신속하게 변경사항을 반영합니다.
4. **디자인 임의 변경 금지**: 모바일, 태블릿 대응 등 반응형 디자인 최적화를 포함하여 기존 UI/UX 디자인을 사용자의 명시적인 요청이나 합의 없이 일방적으로 변경하지 않습니다. 사용자가 직접 적용해보며 수정하는 경우가 많으므로 문제없는 디자인도 임의로 변경해서는 안 됩니다.
5. **백엔드 로직 수정 안내 의무**: Supabase Edge Function이나 DB 스키마 등 백엔드 영역의 코드를 수정한 경우, AI는 사용자에게 **"반드시 Supabase 클라우드에 업데이트(배포)해야 반영됩니다"**라는 사실과 배포 명령어(`npx supabase functions deploy <function_name>`)를 친절히 안내해야 합니다.
6. **데이터 구조 및 Hydration 규칙 엄수**: DB에서 불러온 동적 장소 데이터(`savedTrips` 등)는 장소명이 `destination` 컬럼으로 관리되나, `PlaceCard` 등의 UI 컴포넌트는 전역 상태에서 `location.name` 속성을 필수로 요구합니다. 라우팅 로직이나 컴포넌트 상태 설정(`setSelectedLocation` 등) 시, **반드시 원본 DB 객체를 복사한 후 `name: target.destination` 등으로 필수 UI 속성을 매핑(Hydration)** 하는 과정을 거쳐야 합니다. 누락 시 장소 카드가 빈 화면으로 렌더링되는 치명적 버그가 잦게 재발하므로 데이터 흐름 변경 시마다 각별히 주의해야 합니다.
7. 🚨 **갤러리(이미지 검색/페이지네이션) 로직 수정 시 절대 주의**: 
   - 사진 갱신(Refresh) 로직이나 검색어 정제 로직을 변경할 때, **기존에 잘 나오던 사진이 갑자기 0개가 되거나 빈약해지는 문제(regression)가 수차례 반복**되었습니다. 
   - API 검색어 최적화 시 오지/자연경관(예: 에베레스트, 통가) 등은 한글 검색으로 결과가 나오지 않으므로 영문 매핑(`FALLBACK_DICTIONARY`)을 함부로 제거하지 말고 보존해야 합니다.
   - '강제 새로고침(Force Refresh)' 등 페이지네이션 로직을 짤 때는 새로운 결과를 기존 결과에 덮어씌워버리면(Replace) 사진 수가 줄어들어 빈약해 보이게 되므로, 반드시 **기존 사진들에 새 사진들을 누적(Append)** 하는 방식을 유지해야 합니다.

## 5. 최근 수정 사항 및 남은 과제

### 진행 완료된 작업
- **지구본 마커 공간 분포도 균일화 및 풍성도 강화 (진행 완료)**:
  - `travelSpots.js` 내에 존재하던 대양/대륙(예: 남태평양, 아시아 등) 성격의 가짜 마커 12개를 제거하여 실제 여행 목적지만 남기도록 정제했습니다.
  - 지구본 상의 시각적 빈 공간(태평양 한가운데, 아프리카 내륙, 시베리아 오지 등)을 균형 있게 채우기 위해 `citiesData.js`에서 매력적인 이색/유명 여행지 32곳(우유니, 모리셔스, 옐로나이프, 마라케시 등)을 추가 발탁했습니다.
  - 이를 통해 총 마커 개수를 기존 60개에서 **80개**로 확장하여, 밀집도를 쾌적하게 유지하면서도 전 세계 어디든 풍성한 선택지를 제공하도록 개선했습니다.
  - 마커로 승격된 32곳의 데이터는 `citiesData.js`에서 완전히 삭제하여 지구본 상의 아이콘-텍스트 중복 렌더링(데이터 겹침)을 원천 차단했습니다.
- **갤러리 강제 새로고침(Force Refresh) 기능 정상화 및 페이지네이션 도입 (진행 완료)**:
  - 하드코딩된 `page=1`로 인해 동일 사진만 리프레시되는 현상을 `pageRef`를 통해 수정하고, 결과가 적을 때 덮어씌워져 갤러리가 빈약해지는 문제를 해결하기 위해 **기존 사진을 유지한 채 새 사진을 이어붙이는(Append) 방식으로 전면 개편**했습니다.
- **장소 갤러리 검색어 정제 로직 고도화 (진행 완료)**:
  - 에베레스트, 트리스탄 다쿠냐, 칼라 타파르 등 극지방/오지 검색 시 영어 지명(`name_en`)에 쉼표(`,`)가 포함된 문장을 첫 단어만 추출해 검색하여 정확도 대폭 향상 및 누락 해결.
  - 영어 이름이 누락된 장소들이 한글로 검색되어 결과가 증발하는 문제를 막기 위해 **`FALLBACK_DICTIONARY`를 복구하고 단어 매핑을 강화**했습니다.
- **DailyReport 페이지 전체 라이트 테마 적용 (진행 완료)**:
  - 50대 사용자의 피드백을 반영하여 시인성 및 가독성 개선을 위해 `DailyReport` 내 모든 레이아웃 및 컴포넌트(`DailyLayout`, `Sidebar`, `Dashboard`, `Write`, `AICurationCard`, `CalendarCard`, `RecentList`, `Detail`, `PublicViewer` 등)를 밝은 톤의 라이트 테마로 전면 개편했습니다.
  - 본문 글자 색상을 어둡게(`text-gray-800`), 두께를 `font-medium`으로 조정하여 가독성을 극대화했습니다.
- **SEO 및 사용자 경험(UX)을 위한 URL 구조 개편 (진행 완료)**:
  - `citiesData.js`에 `slug` 필드를 일괄 추가하고, 앱 전체의 라우팅 및 탐색 로직이 숫자 ID나 좌표 대신 이 `slug`를 최우선으로 사용하도록 수정했습니다.
  - 이제 모든 장소는 `/place/new-york`과 같이 명확한 영문 주소를 갖게 되어 가독성과 검색 엔진 최적화가 향상되었습니다.
- **Pexels API 연동 및 고도화된 이미지 Fallback 전략 구현 (진행 완료)**:
  - 기존의 불안정한 Google Custom Search API를 Pexels API로 성공적으로 교체했습니다.
  - Unsplash를 주력 소스로 사용하되, 검색 결과가 부족할 경우에만 Pexels의 고품질 이미지를 병합하는 Fallback 로직을 적용하여 갤러리 사진의 질과 양을 모두 확보했습니다.
- **집단 지성 기반의 갤러리 큐레이션 시스템 도입 (진행 완료)**:
  - 사용자가 직접 사진을 '좋아요'하거나 '숨김' 처리할 수 있는 기능을 구현했습니다.
  - '좋아요' 사진은 갤러리 상단에 영구적으로 고정되며, '숨김' 사진은 다시 노출되지 않습니다.
  - 새로고침(Force Refresh) 시에도 기존의 큐레이션 정보가 지능적으로 병합되어, 집단 지성의 결과가 유실되지 않도록 견고하게 설계했습니다.
- **자잘한 버그 수정 및 안정성 강화 (진행 완료)**:
  - 갤러리 뷰에서 첫 번째 사진을 볼 때 우측 화살표 버튼이 사라지는 버그를 수정했습니다.
  - 경도(lng)가 `0`인 지역(남극점 등)을 클릭했을 때 라우팅이 실패하는 오류를, `undefined` 여부를 명확히 검사하도록 로직을 수정하여 해결했습니다.
- **극한 오지 장소 사진 검색 누락 버그 해결 및 보완 로직 구현 (진행 완료)**
- **갤러리 강제 새로고침(Force Refresh) 기능 도입 (진행 완료)**
- **장소 카드 모바일/반응형 UI 최적화 (진행 완료)**
- **UI 반응형 개선 (진행 완료)**
- **AI 큐레이션 생성 기능 및 데이터 모델 일관성 고도화 (진행 완료)**
- **URL 매칭 견고성 향상 (진행 완료)**
- **UI 개선 (모바일 연관 탐험지 푸터)**
- **버그 수정 (모바일 푸터)**
- **버그 수정 (큐레이션 즐겨찾기)**
- **전반적인 성능 최적화 (진행 완료)**
- **지구본 시각화 및 데이터 구조 개선 (진행 완료)**
- **보안 강화 및 API 통신 구조 개선 (진행 완료)**
- **장소 데이터 정규화 및 검색 정확도 향상 (진행 완료)**
- **지구본 렌더링 성능 최적화 및 데이터 병합 (진행 완료)**
- **지구본 오클릭 및 튕김 현상 방지 (진행 완료)**
- **스마트 검색창 AI 자동 교정 엔진 및 AI 모델 503 오류 최적화 (진행 완료)**
  - 검색창에서 사용되던 `gemini-2.5-flash-lite` 모델이 503 오류를 반환하며 앱 크래시를 유발하는 문제를 파악하여 `gemini-2.5-flash` 모델로 교체했습니다.
  - `apiClient.js`의 `fetchGeminiResponse`에 503, 404 에러 발생 시 가장 안정적인 `gemini-2.5-flash` 모델로 자동 롤백 및 재시도하는 방어 로직(Fallback)을 추가했습니다.
- **구글 Custom Search API 연동 한계 확인 및 대안(Pexels) 전환 결정 (진행 완료)**
- **로고 패널 즐겨찾기 클릭 시 빈 장소 카드 렌더링 버그 수정 (진행 완료)**:
  - URL 라우팅을 `slug` 기반으로 변경한 이후, 로고 패널에서 `TRAVEL_SPOTS`에 없는 DB 저장 동적 장소(즐겨찾기 항목)를 클릭하면 `name` 속성 누락으로 인해 빈 장소 카드가 열리는 문제를 해결했습니다.
  - `Home/index.jsx`의 라우팅 매칭 `useEffect` 내에서 `savedTrips` 데이터를 가져올 때, 원본 객체(`target.destination`)를 기반으로 필수 UI 속성(`location.name`)을 명확히 채워넣도록 **Hydration 로직을 추가**했습니다.

- **계정 및 블로그 혼합 노출(데이터 격리 누락) 버그 해결 (진행 완료)**:
  - 카카오 로그인 등 환경에서 내 블로그에 다른 사람의 블로그가 함께 보이던 치명적인 문제를 파악하여 조치했습니다.
  - `useDashboardData.js`에서 대시보드 데이터를 불러올 때 `eq('user_id', user.id)` 조건을 추가해 본인의 기록만 불러오도록 필터링 로직을 수정했습니다.
  - `Detail.jsx` 및 `Write.jsx`에 URL 직접 입력을 통한 타인 기록 접근/수정/삭제를 차단하기 위해 `user_id` 검증 로직(보안 강화)을 추가했습니다.
- **인증(Auth) 페이지 전면 리디자인 (진행 완료)**:
  - `Login`, `SignUp`, `ForgotPassword`, `UpdatePassword` 컴포넌트를 기존의 어두운 다크 모드에서 **외부 배경은 편안한 다크 네이비(`bg-slate-800`), 내부 카드는 가독성 높은 라이트 모드(`bg-white/95`)**의 글래스모피즘(Glassmorphism) 테마로 개편했습니다.
  - 패딩 축소(`p-6`), 폰트 및 마진 미세 조정을 통해 모바일 환경에서도 스크롤 없이 한 화면에 들어오도록(Scroll-Free) 컴팩트 레이아웃으로 최적화했습니다.
  - 회원가입 버튼 텍스트를 "회원가입 완료"에서 행동을 명확히 유도하는 "회원가입 하기"로 수정하여 사용자 오해를 방지했습니다.

- **AI LogBook(에세이) 프롬프트 고도화 및 퀄리티 개선 (진행 완료)**:
  - 기존의 상투적이고 오글거리는 AI 클리셰("시간이 멈춘 듯한", "완벽한 조화" 등)를 엄격히 금지하고, 사용자 입력 팩트(동행인, 날씨 등)를 왜곡하지 않도록 프롬프트 가이드라인을 강화했습니다.
  - "섬세하고 세련된 브런치(Brunch) 작가" 페르소나를 부여하여, 억지스러운 감정 나열 대신 구체적이고 감각적인 묘사를 통해 인간다운(Human-like) 담백한 문체를 구사하도록 최적화했습니다.
  - ※ *AI 기능별 사용 모델 전략을 재정비하고 `plans/ai-model-strategy.md`에 문서화했습니다.*

- **리포트(LogBook) 삭제 로직 완전 삭제(Hard Delete)로 전환 (진행 완료)**:
  - 기존의 소프트 딜리트(`is_deleted: true`) 방식을 버리고, 삭제 시 DB에서 레코드를 즉각 영구 삭제(`delete()`)하도록 변경했습니다.
  - 대시보드 리스트(`useDashboardData.js`)에서도 삭제된 항목이 렌더링되지 않도록 필터링을 보완했습니다.

- **수정 페이지(Write.jsx) 뒤로 가기 및 라우팅 에러 해결 (진행 완료)**
  - 리포트 작성/수정 후 취소 또는 뒤로 가기 시 예전 경로인 `/report` 로 잘못 라우팅되는 현상을 모두 수정했습니다.
  - 프로젝트 전체(`Detail.jsx`, `Write.jsx` 등)의 `/report` 잔재 코드를 찾아 `/blog` 로 완벽하게 일괄 마이그레이션 적용 완료.
- **배포 중 한글 인코딩 및 빌드 파싱 오류 해결 (진행 완료)**:
  - `src/shared/Auth/Login.jsx`, `src/pages/Home/index.jsx`, `src/components/PlaceCard/modes/PlaceCardExpanded.jsx`, `src/components/PlaceCard/views/*` 등 다수의 파일에서 발생한 한글 텍스트 깨짐(`?`) 문제 해결.
  - 이로 인해 Vercel 배포 시 나타나던 `Unterminated string literal` 및 `Expected ">" but found...` 에러를 원천 차단하고 `npm run build` 성공 확인 완료.
- **URL 구조 개편 (SEO 및 UX/딥링킹 최적화) (진행 완료)**:
  - 사용자에게 기계적으로 느껴지던 `/report` 관련 모든 라우팅 경로를 친숙하고 SEO에 유리한 `/blog`로 일괄 변경했습니다. (App.jsx, Navigate, Link 등 전면 수정)
  - 장소 카드(PlaceCard)의 하위 탭(갤러리, 비디오, 백과)이 단순 React State로 관리되던 것을 `useSearchParams`를 활용하여 URL Query Parameter(`?tab=gallery|video|wiki`)로 동기화했습니다. 이제 특정 탭을 딥링킹(Deep Linking)하여 공유할 수 있고, 뒤로 가기 액션도 자연스럽게 동작합니다.

- **장소 카드(PlaceCard)의 국가/대륙 키워드 누락 버그 수정 (진행 완료)**:
  - `citiesData`에 속한 장소(예: 길리메노, 크라이스트처치, 아스타나 등)를 URL이나 새로고침으로 진입할 때, 확장 장소 카드 상단에 국가명이 표시되지 않는 문제를 해결했습니다.
  - URL 파싱을 담당하는 `Home/index.jsx`의 라우팅 매칭 로직에서 `citiesData` 항목을 객체화(Hydration)할 때 `country`, `country_en` 속성이 누락되어 있던 것을 복사하도록 수정했습니다.
  - 검색이나 지오코딩을 통해 진입할 때는 `useHomeHandlers.js`에서 정상적으로 국가명을 입력받고 있었으나, URL 직접 진입 시에만 발생하는 문제였음을 확인했습니다.
  - `PlaceChatPanel.jsx` 및 `PlaceCardCompact.jsx` UI 컴포넌트에서도 `location.country`가 없을 경우를 대비해 "Explore" 또는 "Global" 등의 Fallback 기본값을 적용하여 써머리 카드와 일관성을 맞추었습니다.

- **장소 카드 로그북(블로그) 탭 UI/UX 전면 개편 및 커뮤니티 기능 도입 (진행 완료)**:
  - **목적**: 기존에 로그북 탭 클릭 시 별도의 `/blog` 페이지로 이탈하던 UX를 개선하여, 장소 카드 내에서 매끄럽게(In-Place) 다른 사람들의 리뷰를 읽고 내 경험을 작성할 수 있도록 합니다.
  - **핵심 UI**:
    - **미니 피드(Mini Feed)**: 해당 장소에 대해 작성된 리뷰들이 카드 내에 스크롤 가능한 피드 형태로 나열됩니다. (`LogbookTab.jsx`)
    - **모달 작성(Modal Editor)**: [✍️ 작성하기] 버튼 클릭 시 화면 중앙에 에디터 오버레이 모달(`ReviewEditorModal.jsx`)이 떠서 장소를 이탈하지 않고 즉시 리뷰를 남길 수 있습니다.
  - **데이터베이스 구조 개편**:
    - 기존 일기장 용도의 `saved_trips` 테이블과 완전히 분리하여 다중 사용자 기반 리뷰 시스템을 위한 **`place_reviews` 전용 테이블을 신설**하고, 안전한 `Row Level Security(RLS)` 보안 정책을 적용했습니다.
  - **주요 기능**:
    - **별점 및 필터링**: 5점 만점의 별점을 부여하고, 장소 전체 평균 별점을 제공합니다. 또한 '전체 보기 / 내 리뷰' 탭 필터 기능을 추가했습니다.
    - **사진 업로드 및 상태 동기화**: `usePlaceReviews.js` 훅을 통해 데이터를 비동기 처리하며, 리뷰 생성/수정/삭제 시 새로고침 없이 즉시 피드 UI에 동기화되도록 구현했습니다.

- **장소 카드 리뷰 시스템 기능 고도화 (진행 완료)**:
  - **AI 리뷰 자동 작성**: `gemini-2.5-flash` 모델을 활용하여 사용자가 입력한 별점과 짧은 키워드만으로 문맥을 이어가며 가독성 좋은(단어/문단 줄바꿈 최적화) 리뷰 초안을 작성해주는 기능을 구현했습니다.
  - **사진 업로드 10장 확대**: 기존 5장에서 10장까지 다중 업로드(`multiple` 속성 지원)가 가능하도록 수정하고 모바일 스크롤 UI를 최적화했습니다.
  - **UI/UX 개선**: 리뷰 텍스트가 3줄을 초과할 경우 `더보기/접기` 버튼을 제공하고, `break-keep` 속성으로 한국어 단어가 잘리지 않게 가독성을 높였습니다. 썸네일 클릭 시 전체 화면으로 볼 수 있는 갤러리 뷰어(Lightbox)를 도입했습니다.
  - **렌더링 안정성 확보**: Lightbox와 작성 에디터 모달을 `createPortal`을 사용하여 `document.body` 최상단에 렌더링함으로써 모바일(PWA) 헤더나 부모 컴포넌트의 CSS(backdrop-blur 등)에 갇히는 버그를 원천 해결했습니다.

- **장소 카드 리뷰(Logbook) '좋아요(하트)' 및 '조회수' 기능 DB 연동 작업 (진행 완료)**:
  - `place_review_likes` 다대다 연결 테이블을 신설하여 동일 사용자의 중복 하트 클릭 방지 로직(RLS 보안 포함)을 설계했습니다.
  - 조회수(`views_count`) 업데이트 시 동시성 이슈를 막기 위한 원자적 증가 RPC 함수(`increment_review_view`)를 작성하여 연동했습니다.
  - `LogbookTab.jsx` 및 `usePlaceReviews.js`에서 실제 하트 수, 내가 누른 상태 확인, 조회수를 반영하며 즉각적인 반응성(Optimistic UI Update)을 추가했습니다.
  - 비로그인 사용자의 잘못된 접근 방지를 위해 권유형 안내문("로그인 하고 리뷰에 참여하고 경험을 공유하세요.")을 적용했습니다.

- **AI (Gemini) API 범용 프록시 서버 마이그레이션 계획 수립 (진행 완료)**:
  - 기존 클라이언트 직접 호출 방식의 보안/안정성 문제를 해결하기 위해 `plans/gemini-proxy-migration-plan.md` 에 Supabase Edge Functions 기반의 점진적 이전 아키텍처 문서를 작성했습니다.

- **비로그인 사용자 대상 로그북(대시보드) 전체 공개 피드 전환 및 UX/UI 최적화 (진행 완료)**:
  - `/blog` 라우트를 `AdminLayout` 보호 범위에서 분리하여 비로그인 사용자도 진입 가능하게 수정.
  - 비로그인 상태 진입 시 DB의 `is_public=true`인 모든 사용자의 전체 공개 기록을 보여주도록 데이터 패칭 로직(`useDashboardData.js`) 개편.
  - 비로그인 상태일 때, 사이드바를 사용자 프로필/메모 대신 **지역 기반 네비게이션(PublicNav)**으로 전환하여 필터링(`?location=도시명`) 제공.
  - 공개 피드에서 타인의 글을 클릭했을 때 관리용 뷰(`/blog/:id`)가 아닌 안전한 **읽기 전용 뷰(`/p/:id`)**로 라우팅되도록 예외 처리.
  - 비로그인 사용자의 원활한 글쓰기 유도를 위해 대시보드 상단에 로그인 배너 추가 및 글 작성(`Write.jsx`) 진입 시 로그인 페이지로 자동 리다이렉트 후 복귀하도록 구현.
  - 추가적인 최적화를 위한 계획서(`plans/logbook-optimization-plan.md`) 작성 완료.

- **장소 카드 리뷰(Logbook) 에디터 버그 수정 및 사용성 고도화 (진행 완료)**:
  - **이미지 업로드 최적화 및 튕김 해결**: `browser-image-compression`을 도입하여 클라이언트 단에서 업로드 전 이미지를 1MB 이하(1920px)로 자동 압축. 5MB 제한 에러를 원천 해결하고, `Promise.all` 병렬 방식을 `for...of` 순차 업로드로 변경하여 모바일 다중 업로드 시의 메모리/네트워크 부하(튕김 현상) 방지.
  - **리뷰 피드 초기 로딩 UX 개선**: `usePlaceReviews.js`의 `isLoading` 초기값을 `true`로 변경하여 데이터 로딩 전 "작성된 리뷰가 없습니다"가 깜빡이는 현상 수정. 압축된 이미지 덕분에 피드 렌더링 속도 대폭 향상.
  - **PC 레이아웃 및 에디터 기능 확장**: 모달 크기를 `max-w-lg`에서 `max-w-2xl`로 확장하고 텍스트 창 높이를 키워 PC에서의 답답함 해소. 내용 [초기화(휴지통)] 버튼 및 하단 실시간 글자수(0/1000자) 카운터 도입.
  - **AI 초안 생성 로직 개선**: 기존 내용 끝에 계속 이어붙던(`Append`) 방식에서, 새롭게 덮어쓰고 새로고침(`Replace`)하는 방식으로 변경하여 수정 및 재작성 편의성 증대.
  - **사진 가로 스크롤 및 직관적 UI 제공**: 가려져 있던 스크롤바를 보이게 전환하고 양끝에 이동 화살표(◀ ▶)를 추가해 PC 환경에서 10장의 사진을 쉽게 탐색/삭제 가능하도록 개선. "2/10장 업로드 중..." 등 업로드 진행률 텍스트와 썸네일(대표 이미지) 안내 문구 추가.

### 남은 과제
- [x] **비로그인 사용자를 위한 명확한 '로그인/회원가입' 진입로(Entry Point) UI 신설**:
  - 홈 화면(좌측 하단 사람 아이콘 등)에 '로그인' 텍스트를 명확히 노출하여 접근성 강화.
  - 로그인 상태에 따라 로그인/로그아웃 텍스트 및 기능 동적 변환되도록 상태 동기화 처리 (`HomeUI.jsx`).
  - 장소 카드 리뷰(Logbook) 탭 내부에 비로그인 사용자 전용 안내 배너("로그인하고 첫 리뷰를 남겨보세요! [버튼]") 추가.
- [x] **조회수 고속 증가 버그 해결**:
  - `sessionStorage`를 도입하여 새로고침 및 탭 전환 시 조회수가 +2씩 증가하는 문제 완벽 해결 (Strict Mode 환경 포함).
- [x] **로그인 플로우 이후 원래 화면 복귀 로직 연동 (UX 개선)**:
  - 장소 리뷰 탭 등에서 로그인 화면으로 진입 후 창을 끄거나 로그인을 성공했을 때, 초기 홈 화면으로 튕기지 않고 보던 장소 카드와 탭으로 자동 복귀하도록 `useLocation`의 `state.from` 기능 전역 적용.
- [x] **사용자 피드백 기반 로그북(블로그) 2차 최적화 및 고도화 진행 (완료)**:
  - `plans/logbook-optimization-plan.md` 문서 참고하여 4단계 개편 완료.
  - 퍼블릭 뷰어(`PublicViewer.jsx`) 하단 액션 버튼 추가, 문구 다이어트, 및 좌측 상단 플로팅 뒤로가기 버튼 추가.
  - 대시보드 로그인 배너 완전히 삭제 (기록 남기기 클릭 시에만 유도하여 거부감 제거).
  - 대시보드 메인 레이아웃 전면 개편 (RecentList 꽉 차게 강조, 상단 그래프 제거, AI 큐레이션/캘린더는 '인사이트' 토글 버튼으로 숨김).
  - 사이드바(Sidebar)의 QuickMemo 제거 및 `PublicNav`(여행지 필터) 고정 배치로 탐색성 극대화.
  - 비로그인 `기록 남기기` 버튼 클릭 시 로그인 모달 무한루프 버그(X버튼 누르면 다시 뜨는 현상) 완전 해결.
  - 프로필 슬라이드쇼(`SlideViewer`)에서 내 프로필 사진을 가장 먼저 띄우도록 수정 및 좌우 화살표 버튼 사진 밖으로 이동.
  - 블로그 상세 페이지 체류 시간 증대 및 커뮤니티 기능(출구/좋아요/댓글 등) 추가 검토 (지속 진행)
  - 블로그 에디터(Write) 리치 확장 툴셋 검토 (지속 진행)
- [ ] **각 기능별 AI 사용 지침 만들기 (AI Usage Guidelines)**: 프로젝트 내 다양한 AI 기능(스마트 검색 교정, 에세이 생성, 챗봇 등)에 대해 어떤 모델을 언제, 왜 써야 하는지 기준 확립 및 문서화
- [ ] **AI (Gemini) API 범용 프록시 서버 구축 (점진적 마이그레이션 구현 시작)**
- [ ] 사용자 기기 성능에 따른 지구본 상세도(LOD) 동적 조절 기능 검토
