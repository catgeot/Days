# gateo.kr 프로젝트 컨텍스트 (AI Context)

**통합 버전**: Gemini + Claude 공용
**최종 업데이트**: 2026-04-22
**최신 로그**: [`plans/2026-04-22-project-log.md`](plans/2026-04-22-project-log.md)

---

## 1. 프로젝트 개요
- **서비스명**: gateo.kr
- **설명**: React 기반의 모던 웹 애플리케이션으로, Supabase를 백엔드로 사용하며 3D 시각화 및 AI 기능을 통합한 서비스입니다.

## 2. 주요 기술 스택
- **Frontend**: React 19.2.0, Vite, React Router DOM
- **Styling**: Tailwind CSS, Lucide React
- **Backend & BaaS**: Supabase (`@supabase/supabase-js`)
- **3D & Data Visualization**: `react-globe.gl`, `three`, `@react-three/fiber`, `@react-three/drei`, `mapbox-gl`, `react-map-gl`
- **AI Integration**: `openai`, `@google/generative-ai`
- **State Management**: React Context API
- **Deployment**: Vercel

## 3. 폴더 구조 및 아키텍처
- `src/components/`: 재사용 가능한 UI 컴포넌트
- `src/pages/`: 라우팅되는 페이지 단위 컴포넌트
- `src/context/`: Context API를 활용한 전역 상태 관리 로직
- `src/shared/`: 공통 유틸리티, 커스텀 훅, 상수 등
- `supabase/`: Supabase 관련 설정, 데이터베이스 스키마 및 마이그레이션

### 🛠️ 주요 시스템 및 구조 설계 (참조 필수)
- **🚨 토큰 절약 최적화 (중요)**: 여행지 데이터 분석/검증 시 **반드시 [`src/pages/Home/data/travelSpots-list.json`](src/pages/Home/data/travelSpots-list.json) (8KB, 245개 여행지 간략 정보)을 사용**하세요. [`travelSpots.js`](src/pages/Home/data/travelSpots.js)는 6,000줄 이상의 전체 데이터를 포함하므로 목록 확인용으로는 비효율적입니다. **토큰 절감: 약 90%**
  - 사용 예시: 누락된 여행지 확인, 여행지 ID 검색, 이름/슬러그 확인 등
  - 재생성: `node scripts/extract-travel-spots-list.cjs` 실행
- **하이브리드 AI 모델 라우팅**: 위키는 Gemini 2.5 Pro, 툴킷은 Gemini 3.1 Pro (→2.5 Pro 폴백)로 목적에 맞춰 분리 구동.
- **지도 시스템**: Mapbox GL JS(`react-map-gl`) 적용. 3D 지형 효과(DEM) 및 Parallax UX.
- **갤러리 배경 연동**: `usePlaceGallery` 훅의 캐싱을 활용해 장소 검색 결과 모달 등의 배경으로 Unsplash 이미지가 딜레이 없이 로드됨.
- **SEO 및 라우팅 구조**: `/place/:slug` 형태의 딥링크 정적 라우팅이 적용. 동적 `sitemap.xml`(vite-plugin-sitemap), 캐노니컬 방어(Helmet), GA4 SPA(useLocation) 추적이 완비됨.

## 4. 🤖 AI 어시스턴트 핵심 행동 원칙 (Core Rules)

### 🚨 ARCHITECT 모드 전용 규칙 (Claude Only)
**Architect 모드는 정보 수집 및 간결한 todo 작성에 집중합니다:**
1. **계획서/보고서 파일 작성 금지**: 사용자가 명시적으로 요청하지 않는 한 markdown 계획서 파일을 생성하지 않습니다.
2. **todo 리스트 우선**: 복잡한 작업은 `update_todo_list`로 간결하게 정리합니다.
3. **토큰 낭비 방지**: 긴 코드 예제나 상세한 구현 계획을 문서에 작성하지 않습니다.
4. **즉시 코드 모드 전환**: 정보 수집과 todo 작성이 끝나면 바로 코드 모드로 전환을 제안합니다.
5. **로그 파일 간결성**: 프로젝트 로그는 2-3줄 요약으로만 기록합니다.

### 일반 작업 원칙
사용자의 요청에 따라 코드를 수정하거나 기능을 추가할 때:
1. **논리적 구조 제안**: 수정 및 구현하려는 코드의 논리적 구조와 방향성을 먼저 설명합니다.
2. **사용자 승인**: 제안한 구조에 대해 사용자의 승인을 대기합니다.
3. **직접 파일 수정(Edit Tool 사용)**: 승인이 완료되면 채팅창에 전체 코드를 길게 출력하며 토큰을 낭비하는 대신, AI가 에디터의 파일을 직접 수정(Edit)하여 신속하게 변경사항을 반영합니다.
4. **디자인 임의 변경 금지 및 UI 밀도 원칙**: 모바일, 태블릿 대응 등 반응형 디자인 최적화를 포함하여 기존 UI/UX 디자인을 사용자의 명시적인 요청이나 합의 없이 일방적으로 변경하지 않습니다.
   - **스크롤 최소화 및 중요 버튼 누락 방지 원칙**: 장소 리뷰 등 작성하기/편집창 뷰를 구성할 때 전체 스크롤을 최소화(Scroll-Free)하여, 패딩/글자크기/버튼크기를 최적화.
   - **타이포그래피 및 단어 끊김 방지 원칙**: 한국어 텍스트가 렌더링되는 문단에는 반드시 CSS 속성(`word-break: keep-all;` 또는 Tailwind 클래스 `break-keep`)을 명시적으로 적용.
5. **백엔드 로직 수정 및 배포 의무**: Supabase Edge Function 수정 시 터미널을 통해 AI가 직접 배포를 우선 시도합니다. 배포 명령어 작성 시 Project Ref가 필요하므로, 배포 전 반드시 `type .env.local` 명령어로 `VITE_SUPABASE_URL`을 확인하고 해당 URL의 도메인 첫 부분(Project Ref)을 추출하여 `npx supabase functions deploy <function_name> --project-ref <추출한_ID> --no-verify-jwt` 형태로 명령을 실행하세요. (프론트엔드 통신 시 권한 에러(401) 방지를 위해 `--no-verify-jwt` 플래그 필수)
6. **데이터 구조 및 Hydration 규칙 엄수**: DB에서 불러온 장소 데이터(destination)를 `PlaceCard` 컴포넌트에 넘길 때, **반드시 원본 DB 객체를 복사한 후 `name: target.destination` 등으로 매핑(Hydration)** 하여 빈 화면 에러를 방지합니다.
7. 🚨 **갤러리(이미지 검색/페이지네이션) 로직 수정 시 절대 주의**: API 검색어 최적화 시 영문 매핑(`FALLBACK_DICTIONARY`)을 함부로 제거하지 말고, 갱신(Refresh) 시 **기존 사진들에 새 사진들을 누적(Append)** 하는 방식을 유지해야 합니다.
8. **작업 단위 분할 및 커밋 주도 원칙 (Commit-Driven Workflow)**:
   - 🚨 **사용자 확인 필수 원칙 (절대 규칙)**: 코드 수정 후 반드시 사용자의 테스트 및 확인을 대기하며, 임의로 완료를 선언하지 않습니다.
   - 단위 작업 완료 시 `git commit -m "커밋 메시지"`를 제안.
9. **한글(영문) 고유명사 표기 규칙**: 장소나 상호명 생성 시 항상 `한글명('현지 영문명')` 형식을 엄수.
10. **계획 문서 작성 원칙 (Documentation Principle)**: 🚨 **사용자가 명시적으로 요청하지 않는 한 계획서/보고서 파일을 생성하지 않습니다.** 필요시 로그 파일에 2-3줄로 간결하게 기록합니다.

## 5. 최근 수정 사항 및 남은 과제

### 🔥 진행 완료된 최신 작업
- **[Phase 8-9] 탐색 페이지 큐레이션 다양성 대폭 개선 (2026.04.22)** ⭐
  - **타겟 여행지 3배 확장**: 39개 → 115개 (가족 38개, 장거리 42개, 휴양 35개)
  - **일일 셔플 시스템**: 타겟 배열 자체를 매일 셔플하여 매일 다른 우선순위로 표시
  - **지루함 해소**: 115개 타겟 중 매일 30개씩 로테이션으로 신선한 UX 제공
  - **신규 여행지 반영**: 제주, 헬싱키, 레이캬비크, 부다페스트, 브리즈번 등 큐레이션 추가
  - **커밋**: `8e43aa8` - SearchDiscoveryModal.jsx 큐레이션 로직 개선
- **[Phase 8-9] 누락 여행지 25개 추가 완료 (2026.04.22)** ⭐
  - **관광 매력도 검토**: 이시가키, 방비엥 포함 25개 전체 추가 결정 (완벽한 커버리지)
  - **추가된 여행지**: ID 341-365 (국내 2개, 일본 9개, 동남아 4개, 유럽 5개, 북미 2개, 오세아니아 2개)
  - **검증 완료**: 220개 → 245개 증가, 트립링크 패키지 매핑 누락 0개 달성
  - **토큰 절약**: `travelSpots-list.json` (8KB) 생성으로 향후 분석 작업 시 90% 토큰 절감
  - **커밋**: `98b376f` - travelSpots.js + travelSpots-list.json 업데이트
- **[Phase 8-9] travelSpots.js 누락 여행지 분석 완료 (2026.04.22)** ⭐
  - 트립링크 패키지 256개 매핑 여행지와 travelSpots.js(220개) 비교 분석
  - **실제 누락된 주요 도시 25개 추출**: High(8), Medium(14), Low(3) 우선순위 분류
  - **토큰 절약 최적화 스크립트 개발**: `extract-travel-spots-list.cjs` - 6,000줄 대신 220줄 JSON으로 여행지 목록 관리
- **[Phase 8-9] 트립링크 패키지 전면 확장: 35개 패키지 통합 완료 (2026.04.22)** ⭐
  - **1차 배치 (13개)**: 홍콩/마카오, 대만, 도쿄, 오사카/간사이, 쿠슈/후쿠오카, 혼슈, 대마도, 동유럽(업데이트), 북유럽, 아프리카, 몽골/내몽골, 오키나와 패키지 추가.
  - **2차 배치 (9개)**: 국내여행(제주도) 신규 카테고리 신설, 북미(미국/캐나다), 미동부, 미서부, 호주, 중남미, 하와이, 괌, 사이판 패키지 개별 분리.
  - **여행지 매핑 대폭 확장**: 200+ 도시/지역 매핑 완료 (일본 60+, 북미 30+, 하와이/호주/중남미 등).
  - **패키지 구조**: domestic(1), family(13), longhaul(11), resort(10) 총 4개 카테고리로 체계화.
  - **동작 구조**: 기존 `TripLinkDynamicBanner`와 `TripLinkModal` 시스템이 모든 신규 패키지를 자동 지원하도록 완벽 연동.
  - **커밋**: `1f32f74` (1차 배치), `42bc62b` (2차 배치)
- **[Phase 8-8] Direct Ferries 페리 예약 위젯 통합 완료 (2026.04.21)** ⭐
  - Direct Ferries 파트너 코드(`F8350KR`) 승인 및 통합 완료.
  - **URL 파라미터 테스트**: `oprt`/`rprt`, `ctry` 파라미터가 작동하지 않아 기본 iframe + 추천 노선 텍스트 방식으로 구현.
  - **DirectFerriesWidget.jsx 신규 개발**: iframe 위젯(285px) + 여행지별 추천 노선 박스 통합.
  - **추천 노선 데이터 구조**: Direct Ferries 실제 검색 리스트 형식(`한글(영문:English)`)으로 9개 국가 주요 페리 노선 매핑 완료.
  - **전체 통합**: `ToolkitCard.jsx`(페리 카드 위젯), `JourneyTimeline.jsx`(상세 여정 플래너 버튼), `utils.js`(페리 예약 링크) 모두 Direct Ferries로 변경.
  - **URL 파라미터**: `targ=1` 설정으로 검색 결과가 새 창에서 열리도록 최적화.
  - **한국어 홈 링크**: `https://www.directferries.co.kr/?dfpid=7263&affid=1001&rurl=` (파트너 ID 연동)
  - **커버리지**: 크로아티아, 그리스, 한일 페리, 이탈리아, 태국, 스페인, 인도네시아, 필리핀, 영국 총 9개 국가 주요 노선.
- **[Phase 8-8] 트립링크 Phase 2 패키지 배너 데이터 전면 보강 완료 (2026.04.20)** ⭐
  - **신규 패키지 추가**: `tripLinkPackages.js`에 인도, 인도네시아(발리), 라오스, 필리핀(세부/보라카이 등), 태국(방콕/푸켓 등), 중국(북경) 총 6개 지역의 배너/모달 adKey 연동.
  - **기존 패키지 보강**: 베트남(다낭), 일본(홋카이도), 서유럽(파리), 동유럽(프라하), 남태평양(괌), 말레이시아(코타키나발루) 6개 패키지의 728x90 배너 adKey(`bannerAdKey`) 추가하여 데스크톱/모바일 분리 운영 구조 완성.
  - **키워드 매핑 확장**: `tripLinkDestinationMap.js`에 신규 패키지들의 한/영 도시 키워드 매핑 및 국가/대륙별 폴백(`FALLBACK_PACKAGE_MAP`) 로직 최적화.
- **[Phase 8-8] 장소 카드 위키/챗 패널 버튼 레이아웃 최적화 (2026.04.20)** ⭐
  - 챗 패널 플래너 탭 활성화 시 나타나는 "패키지 특가" 버튼을 데스크톱 환경에서만 노출되지 않도록 `md:hidden` 처리 (플래너 탭 내부 버튼과의 중복 해결).
  - 모바일 환경 위키 본문 읽기 경험 개선을 위해 `PlaceWikiDetailsView` 하단에 플로팅 바를 배치하고, 제미나이 호출 버튼과 패키지 버튼을 나란히(5:5) 배치하여 접근성 상향.
  - 위키 네비게이션 및 디테일 하단 버튼들의 패딩, 높이(`min-h-[44px]`), 아이콘 크기를 다이어트하여 모바일 및 PC 환경 모두에서 세련되고 컴팩트한 디자인으로 개선.
- **[Phase 8-8] 트립링크 모바일 UX 개선 및 PC 드래그 스크롤 (2026.04.20)** ⭐
  - 모바일 트립링크 모달 이미지 로딩 버그 수정: IntersectionObserver 딜레이 제거로 첫 진입 시에도 이미지 정상 로드
  - PC 큐레이션 카드 마우스 드래그 스크롤 기능 추가: 클릭 & 드래그로 카드 영역 이동 가능
  - 모바일 모달 레이아웃 개선: 전체 화면 활용 및 iframe 원본 크기 유지
- **[Phase 8-8] 트립링크 Phase 2 계획 수립: 여행지 키워드 동적 매핑 및 PlaceCard 통합 (2026.04.20)** ⭐
  - `plans/triplink-phase2-placecard-integration.md` 상세 계획 문서 작성 완료: 여행지별 패키지 자동 매칭 시스템 설계, PlaceCard 위키탭/플래너탭 버튼 추가 계획, 데이터 구조 및 유틸리티 함수 설계.
  - **여행지 키워드 동적 매핑 전략 확정**: `tripLinkDestinationMap.js`에 여행지명(한글/영문) → 패키지 ID 매핑 테이블 설계. `tripLinkMatcher.js` 유틸리티 함수로 장소 객체 기반 패키지 필터링 로직 구현 예정.
  - **PlaceCard 통합 계획**: 위키탭 하단에 "제미나이 최신 정보 요청" 버튼과 나란히 "패키지 상품 보기" 버튼 추가. 플래너탭에서 "앱으로 여정 보내기" 버튼을 제거하고 "패키지로 간편하게 준비하기" 버튼으로 교체.
  - **모달 재사용 구조**: `TripLinkModal`을 PlaceCard 레벨(`PlaceCardExpanded`)로 이동하여 위키탭/플래너탭에서 공통으로 사용할 수 있도록 설계.
  - **다음 단계**: 사용자가 트립링크에서 지역별 iframe 링크를 수집하면 매핑 데이터를 입력하고, 코드 모드로 전환하여 실제 구현 진행 예정.
- **[Phase 8-8] 트립링크 네이티브 썸네일 카드 및 대화면 모달 팝업 구현 (2026.04.20)** ⭐
  - 기존 `TripLinkIframeCard`를 대체하여 `TripLinkSectionCard.jsx` 신규 개발: 패키지 대표 키워드(예: 다낭, 파리)의 Unsplash 사진을 배경으로 불러와 기존 여행지 카드(`SpotThumbnailCard`)와 동일한 디자인으로 네이티브 통합. 이질감 완전 해소.
  - `TripLinkModal.jsx` 버그 수정: 모바일 최초 진입 시 모달의 등장 애니메이션(`animate-scale-up`) 도중 iframe 렌더링이 시작되며 반응형 너비 계산이 꼬여 이미지가 줄로 보이거나 찌그러지던 치명적 결함을 파악하고, `setTimeout` 400ms 지연 로드 방식을 도입해 완벽히 해결. 추가로 헤더의 `shrink-0` 및 래퍼 `flex-auto` 최적화.
  - `tripLinkPackages.js` 데이터 구조 확장: 각 패키지에 `targetKeyword`, `title`, `description` 속성을 추가하여 썸네일 카드 렌더링에 활용. 배너 크기를 1024x768로 통일.
  - 제휴 파트너 주체를 "gateo x 노랑풍선"에서 "gateo x 트립링크"로 정확하게 수정하여 어필리에이트 정체성 명확화.
- **[Phase 8-8] 트립링크 Phase 2 여행지-패키지 동적 매핑 및 PlaceCard 연동 구현 완료 (2026.04.20)** ⭐
  - `tripLinkDestinationMap.js` 데이터 구조체 개발 완료: 각 여행지 키워드(한글/영문)와 패키지 ID 맵핑 테이블.
  - `tripLinkMatcher.js` 유틸리티 개발 완료: `location` 객체를 받아 정확한 패키지 또는 대륙/국가 기반 폴백을 반환하는 로직 구축.
  - `TripLinkModal`을 `SearchDiscovery`에서 분리하여 `PlaceCard/modals/` 하위 공통 컴포넌트로 구조 통합.
  - `PlaceCardExpanded`에서 매칭 패키지(`matchedPackage`)를 관리하고 위키(`PlaceWikiNavView`) 및 플래너 탭(`PlannerTab`)으로 하향 전달 구조 개편.
  - **위키 탭 하단**: 기존 제미나이 단독 버튼 대신 "패키지 상품 보기" 버튼을 나란히 분할 배치.
  - **플래너 탭 상단 배너 최적화**: PC/데스크톱 환경에서는 기존 모달 호출용 "패키지로 간편하게 준비하기" 버튼을 제거하고, 그 자리에 즉각적인 전환율을 기대할 수 있는 가로형(728x90, `op19uq`) iframe 배너를 인라인으로 직접 노출하는 방식으로 개편. 단, 모바일 기기에서는 텍스트 시인성 확보를 위해 인라인 배너를 숨기고 기존의 하단 챗 패널 버튼식(클릭 시 `s74kc3` 1024x768 모달 팝업)을 유지하도록 하이브리드 UX 로직 적용. 싱가포르 신규 매핑 데이터 추가.
- **[Phase 8-8] 홈 탐색 모달(`SearchDiscoveryModal`) UI 시인성 개선 및 배너 조화 (2026.04.19)** ⭐
  - **배너 조화 최적화**: 트립링크 동적 배너가 들어간 `TripLinkIframeCard.jsx`의 배경색을 주변 사진 카드(해변, 하늘)와 위화감이 없도록 청명한 하늘색에서 흙/모래 색상으로 떨어지는 그라데이션(`from-sky-400 via-sky-200/80 to-[#d4a373]`)으로 변경하여 디자인 완성도 향상.
  - **맥락적 스크롤 유도 버튼**: `CurationSection.jsx` 우측 상단에 동떨어져 있던 패키지 상품 보기 버튼을 제거하고, 대신 `subtitle` 텍스트 안의 핵심 문구("휴양 패키지 추천" 등)를 파싱하여 인라인 칩 버튼 형태로 제공. 글을 읽다가 자연스럽게 눌러서 횡스크롤(`scrollToAd`) 되도록 UX 개선.
  - **스크롤바 및 이동 버튼 시인성 대폭 개선**: 다크 모드에서 잘 보이지 않던 좌우 내비게이션 화살표 버튼을 화이트 글래스톤(`bg-white/90`)으로 변경 및 크기 확대. 하단 가로 스크롤바(`custom-scrollbar`)의 두께(16px)와 밝기(0.6~0.9)를 대폭 상향하여 현재 위치 파악 용이성 확보.
- **[Phase 8-8] 트립링크 유럽 패키지 배너 추가 및 에디터스 픽 큐레이션 최적화 (2026.04.19)** ⭐
  - 에디터스 픽의 인피드 광고 위치를 기존 2번째, 6번째에서 여행지 카드 5개 렌더링 직후인 6, 7번째에 연달아 노출되도록 `CurationSection.jsx` 배치 로직 변경.
  - 두 번째 카테고리(장거리/유럽) 테마에 가장 대중적인 **'서유럽 핵심 일주'**와 가성비 낭만 상품인 **'동유럽/발칸3국'** 패키지 배너 코드를 연동 (`tripLinkPackages.js`).
  - 세 번째 카테고리(휴양/에어텔) 테마에 **'남태평양'**과 **'코타키나발루/쿠알라룸푸르'** 패키지 배너 코드를 연동하고 노출 타겟 최적화.
  - 사용자의 검색 피로도를 낮추고 접근성을 최적화하기 위해 `SearchDiscoveryModal.jsx` 내 큐레이션 추출 함수(`getSpotsByTargets`)를 개선. 기존의 무작위 셔플 순서가 아닌, `familyTargets`, `longhaulTargets` 배열에 명시된 우선순위(파리, 로마, 오사카 등)를 1차적으로 강제 보장하여 정렬되도록 로직 완벽 변경.
  - 트립링크 배너 사이즈 336x280 변경에 따라, 모바일 기기에서의 레이아웃 오버플로우를 방지하기 위해 `TripLinkIframeCard`의 CSS scale 비율을 0.75에서 0.7로 안정화.
  - 사용자가 횡스크롤을 할 때 배너가 뒤늦게 렌더링되며 스피너가 보이는 UX 저하를 막기 위해, `TripLinkIframeCard`의 `IntersectionObserver` 지연 로딩을 제거하고 즉시 로딩되도록 개선.
- **[Phase 8-8] 트립링크 동적 배너(iframe) 모바일 최적화 및 렌더링 고도화 (2026.04.19)** ⭐
  - 에디터스 픽(탐색창)의 인피드 광고 영역을 정적 이미지에서 트립링크 동적 배너(`iframe`)로 전환 완료.
  - 가로형 배너를 세로형 카드에 억지로 넣을 때 발생하던 레이아웃 오버플로우 및 깨짐 문제를 해결하기 위해, 트립링크 서버에서 300x250 배너 규격을 발급하도록 유도.
  - `TripLinkIframeCard.jsx`를 신규 개발하여 기존 여행지 카드 디자인(모바일 240x320)을 유지하면서 내부의 300x250 iframe을 CSS `transform: scale()`로 완벽하게 축소 및 융화시킴.
  - 불필요한 플랫폼 UI 훼손(상단 긴 KFTC 문구 등)을 롤백하고, 카드 내부에 'AD', '트립링크 특가' 뱃지를 표시하여 공정위 규정을 깔끔하게 충족.
- **[Bug Fix] 지오코딩 검색 결과 우선순위 필터링 (스코어링) 도입 (2026.04.18)** ⭐
  - 지오코딩 API (Nominatim) 검색 시, 대도시의 단순 행정구역(예: 오사카 미야코지마 구)이 실제 관광지(오키나와 미야코지마 섬)보다 우선 반환되어 엉뚱한 정보가 노출되는 이슈 해결.
  - `geocoding.js` 내에 응답 결과를 분석하는 `calculatePlaceScore` 함수를 도입하여 `island`, `city`, `tourism` 키워드에는 가점을, `suburb`, `station` 등에는 감점을 부여하여 실제 여행지(섬, 도시)가 최우선으로 선택되도록 재정렬 로직 구현.
- **[Phase 8-8] 트립링크(TripLink) 연동: 홈 탐색창 인피드(In-feed) 큐레이션 고도화 (2026.04.18)** ⭐
  - 기존 자유여행 카드만 있던 홈 탐색 창(`SearchDiscoveryModal`)에 상업적인 패키지 상품 리스트 노출 시 발생할 수 있는 유저 거부감을 최소화하기 위해 **하이브리드 인피드 광고 방식** 도입.
  - 기획서에 맞춰 큐레이션 테마 3종(가족/효도, 장거리/유럽, 에어텔/올인클루시브) 제목 및 문구 수정.
  - 각 테마에 맞는 목적지를 `familyTargets`, `longhaulTargets`, `resortTargets`로 명시하여 큐레이션 데이터 추출 로직 하드코딩 필터링 완료.
  - `src/pages/Home/data/tripLinkPackages.js` 파일을 신설하여 테마별 제휴 링크 데이터 구조체(2개씩) 템플릿 세팅.
  - 기존 카드 베이스에 외부 링크로 이동하며 배지를 포함한 `PackageThumbnailCard.jsx` 신규 컴포넌트 개발.
  - 횡스크롤 여행지 리스트 중 **2번째(index 1)와 6번째(index 5)** 자리에 네이티브 패키지 카드가 자연스럽게 삽입되도록 `CurationSection.jsx` 연동 완료.
  - `travelSpots.js` 내에 기존에 누락되었던 핵심 패키지 타겟 목적지 및 사용자 추가 요청 장소 총 20개(다낭, 삿포로, 북해도, 미야코지마, 코토르 등)를 신규 DB로 추가하여 탐색 오류를 방지함.
  - 트립링크 특가 상품을 위한 핫딜 배너 팝업을 기획하고 개발 테스트를 거쳤으나, 사용자의 "홈 화면 조용한 탐색 컨셉 유지" 피드백을 반영하여 최종 노출 계획을 롤백(취소)함.
- **[Phase 8-7] 플래너 탭 지역별 짐 보관 서비스 Bounce 글로벌 배너로 단일화 (2026.04.17)** ⭐
  - 복잡하게 분산되었던 `ecbo cloak`, `Radical Storage` 등의 지역별 동적 매핑 로직 제거.
  - 전 세계 커버리지를 가진 `Bounce` 제휴 링크 배너로 일괄 통일하여 수익 파이프라인 일원화 및 유지보수 단순화.
- **[Phase 8-8] 트립링크(TripLink) 패키지 배너 연동 기획 및 마스터 플랜 수립 (2026.04.17)** ⭐
  - 트립링크 노랑풍선 패키지 연동을 위한 노출 타겟 분석 및 다각도 전략 수립 (`plans/triplink-integration-master-plan.md` 파일에 통합).
  - 홈 화면의 기존 3가지 테마를 패키지 타겟(가족/효도, 장거리/안전, 휴양/올인클루시브)으로 전면 개편 기획.
  - 마감 임박 특가, 골프 등 수명주기가 짧은 상품은 홈 우측 상단 고정 배너/팝업 형태로 분리하기로 확정.
  - *플래너 탭 맥락형 동적 링크 매핑은 트립링크 문의 답변 대기 상태로 일시 보류.*
- **[Security Fix] AI 할루시네이션 URL 생성 방어 및 악성 링크 차단 (2026.04.16)** ⭐
  - 네팔 에베레스트 여행지에서 "입산 허가" 버튼 클릭 시 도박 사이트로 연결되는 심각한 보안 이슈를 발견하고 긴급 대응.
  - 네팔 이민국 링크 키워드에 '에베레스트', 'EBC', '베이스캠프' 추가하여 정확한 비자 링크 매칭 보장.
  - `update-place-toolkit` Edge Function 프롬프트에 **URL 생성 규칙**을 명시적으로 추가: "pre_travel 배열의 url은 반드시 공식 정부/이민국 웹사이트만 사용. 입산 허가, 가이드 고용 등 에이전시 대행 항목은 url을 null로 설정하고 advice에만 설명 작성. 임의의 관광 웹사이트/여행사 URL 생성 절대 금지."
  - Edge Function 재배포로 향후 AI가 검증되지 않은 임의 URL을 생성하지 못하도록 원천 차단.
- **[Phase 8-5] 전 세계 공식 비자(e-Visa/ETA) 링크 매핑 전면 확대 및 에러 수정 (2026.04.16)** ⭐
  - 기존 `OFFICIAL_VISA_LINKS` 배열에 탄자니아, 러시아, 볼리비아(SIV 포털), KAZA 유니비자, 요르단 패스, 세이셸 ETA, 사우디아라비아, 라오스, 네팔, 쿠바, 마다가스카르 등 주요 관광 국가 비자/입국서류 신청 공식 포털 신규 추가.
  - 브렉시트에 따른 영국의 독립적인 ETA 제도 도입을 반영하여 기존 유럽 ETIAS 항목에서 영국을 분리하고, 404 에러를 방지하기 위해 공식 단축 URL(`https://www.gov.uk/eta`)로 매핑 완료.
  - 사용자가 세부 도시명(예: 퀸스타운, 룩소르, 세렝게티 등)으로 목적지를 탐색할 때에도 외교부 폴백 없이 정확한 링크가 매칭되도록 30개국 이상의 하위 주요 관광지 키워드를 전면 보강하여 프론트엔드 할루시네이션 완벽 차단.
  - 볼리비아 기존 포털 503 에러, 마다가스카르 e-Visa 사이트 접속 불가 등 실제 사용자 제보를 통해 URL 정확성 검증 및 수정.
- **[Security Patch] 외부 API 도용 방어 및 프록시 마이그레이션 적용 (2026.04.16)** ⭐
  - `VITE_GEMINI_API_KEY` 환경 변수의 클라이언트 노출로 인한 외부 도용 현상을 확인하고, 클라이언트(브라우저)에서 직접 Google API를 호출하던 모든 로직(`useHomeHandlers.js`, `ChatModal.jsx`, `useLogbookAI.js`, `ReviewEditorModal.jsx`, `usePlaceChat.js`)을 전면 제거.
  - 모든 LLM API 호출을 `gemini-proxy` Edge Function을 거치도록 마이그레이션하고, 프록시 내부에 허용 모델 리스트(`ALLOWLIST`)를 추가하여 악의적인 `tts` 모델 등의 사용을 원천 차단.
  - Mapbox 토큰(`VITE_MAPBOX_TOKEN`) 도용 방지를 위해 Mapbox URL Restrictions 정책 적용 가이드를 제공하여 Vercel 도메인 및 로컬 주소만 접근할 수 있도록 보안 강화.
- **[Phase 8-3 & 9] 복잡한 여행지 시스템 연동 및 큐레이션 최적화 (2026.04.15)** ⭐
  - 비자/서류 딥링크 매칭 로직 개선: 경유지 정보 등에서 2개 이상의 비자(예: ESTA, eTA)가 언급될 경우 해당하는 모든 비자 신청 버튼을 동시 렌더링. 단, 할루시네이션 방지를 위해 텍스트 내에 국가명과 비자종류가 함께 존재할 때만 매칭되도록 엄격화 (`utils.js`).
  - AI 숙소 큐레이션 연결 고도화: `data.advice`에 포함된 스마트 링크(`[@지역명@]`) 형태의 타겟별 숙박 지역명을 자동 파싱. 
  - (UX 최적화) 각 줄(line)에서 첫 번째 명칭만 추출하여 버튼 중복 생성을 막고, '주의', '피해야' 등 부정적 텍스트가 포함된 줄은 추천 버튼 생성에서 제외 처리. 
  - 검색 퀄리티 향상을 위해 세부 지역명(예: `뮤리 해변`)과 카드 지명(예: `라로통가`)을 결합한 스마트 검색어(`뮤리 해변 라로통가 숙소`)를 자동 조립하여 마이리얼트립 검색 결과 정확도를 대폭 상승시킴.
  - 여정 타임라인(`JourneyTimeline`) 내 동적 액션 로직 개선: 환승이나 경유 단계에서는 불필요한 '공항 픽업' 버튼이 렌더링되지 않도록 방어 로직 추가.
  - 식당 예약 링크 세분화 및 버그 수정: TheFork 링크의 오작동 유발 검색 쿼리 제거 후 홈 연결, 발데스 반도 등 미매핑 지역에 대해 Klook 식당 통합 검색(`search/result/?query=레스토랑`)으로 안전하게 폴백되도록 수정.
  - Aviasales(Travelpayouts) 사용자 추적 기능 추가: 예약 데이터를 비서 앱과 연동/수집하기 위해 `WhiteLabelWidget.jsx`의 화이트라벨 모달 파라미터에 `origin=SEL` 및 Sub ID(`marker`) 값을 세팅 완료.
- **[Phase 8-7] 플래너 탭(PlannerTab) 전면 컴포넌트 리팩토링 (2026.04.15)** ⭐
  - 920줄 이상 비대해진 `PlannerTab.jsx`의 복잡성을 해소하기 위해 `src/components/PlaceCard/tabs/planner/` 구조로 모듈화.
  - `constants.js` (정적 데이터), `utils.js` (제휴 링크 및 데이터 정제 로직) 분리.
  - `ToolkitCard`, `JourneyTimeline` 등 하위 UI 6개를 개별 컴포넌트 파일로 분할하여 유지보수성 획기적 향상.
- **[Phase 8-7] 마이리얼트립 제휴 링크 적용 확대 및 툴킷 하이브리드 최적화 (2026.04.15)** ⭐
  - 투어/액티비티 영역에 **클룩(글로벌 어트랙션/패스)**과 **마이리얼트립(한국어 워킹투어/가이드)** 버튼을 분리하고 나란히 배치하여 하이브리드 UX 달성.
  - 비자 링크 맵핑 아키텍처를 응용하여 `constants.js` 내에 **짐 보관 서비스 매핑(`LUGGAGE_STORAGE_LINKS`)** 신설. 일본(ecbo cloak), 유럽(Nannybag), 글로벌(Bounce)로 세분화.
  - 플래너 탭 케냐 eTA 링크 매핑 누락 오류 수정 (외교부 폴백 방지).
  - 모바일 환경에서 Travelpayouts 항공권 모달 닫기 버튼이 상단 헤더에 가려지는 UI 버그를 `createPortal` 적용 및 Safe Area(`pt-safe`) 확보를 통해 완벽히 해결.
- **[Phase 8-6] 마이리얼트립 파트너스 API 연동 및 제휴 링크 개편 (2026.04.15)** ⭐
  - 기존 에어비앤비 링크 등을 전환율이 높은 마이리얼트립 파트너스 링크로 교체.
  - `mrt-link-generator` Edge Function을 구축하여 API 호출을 통한 동적 단축 링크 자동 생성 아키텍처 완성.
  - `<MrtDynamicLink />`, `<MrtTimelineAction />` 컴포넌트를 통해 비동기적으로 링크를 렌더링.
- **[Phase 8-5] 스마트 트래블 툴킷 UX 개선 및 제휴 서비스 확장 (2026.04.15)** ⭐
  - 플래너 탭을 3개의 의미 단위(🛫 출발 전 필수 준비, 🛬 현지 도착 및 이동, 🌴 현지 100% 즐기기)로 시각적 섹션화하여 가독성 개선.
  - 여정 타임라인(`JourneyTimeline`) 내 각 단계의 키워드(공항, 페리, 숙소, 항공)를 분석하여 해당 단계 우측에 **동적 [예약하기] 액션 버튼**을 즉시 렌더링.
  - 여행자 보험, 에어비앤비, 짐 보관(Bounce), 식당 예약(TheFork) 버튼 추가 및 모바일 최적화를 위한 텍스트 말줄임(`truncate`) 적용.
  - `OFFICIAL_VISA_LINKS`에 이집트와 인도 e-Visa 포털을 추가하여 비자 할루시네이션(외교부 폴백) 예외 처리 완비.
- **[Phase 8-3 & 9] Planner Hub 연동 고도화 및 교통/패스 제휴 링크 세분화 (2026.04.14)** ⭐
  - 플래너 탭 정보 카드의 노출 순서를 여행 준비 흐름(비자->항공권->숙박->공항픽업->페리->유심->교통->앱->명소->안전)에 맞춰 재배치.
  - AI JSON 파싱 오류(문자열/배열 반환) 방어를 위해 `PlannerTab.jsx` 내 `getAdviceText` 안전 처리 로직 추가.
  - 클룩 다이내믹 위젯은 목적지 주입 불가 한계로 제외하고, 기존 동적 딥링크(버튼) 방식을 유지.
  - 교통 영역의 딥링크를 4종(교통/레일 패스, 렌터카, 일일 투어, 오토바이 대여)으로 세분화하고 오토바이 대여는 BikesBooking 어필리에이트 링크로 변경.
  - 카테고리명 "유심 및 공항픽업"을 "유심 및 와이파이"로 수정 및 "페리/크루즈 예약" 버튼명 변경.
- **[Phase 8] 툴킷 필수 정보(도착 공항, 상세 여정, 공항 픽업) 강제 표시 최적화 (2026.04.14)** ⭐
  - AI 생성 내용 중 특정 항목이 누락되는 현상을 방지하기 위해 `update-place-toolkit` 백엔드 프롬프트 개선.
  - 기계적 필수 작성 지시 대신, "도착 공항 명시: 복귀를 위한 최후의 보루", "상세 여정 플래너: 여행의 큰 그림 제공"과 같이 사용자 관점의 당위성을 설명하여 안정적 JSON 반환 유도.
  - `PlannerTab.jsx`의 '상세 여정 플래너' 컴포넌트를 `is_complex` 복잡도 조건문 밖으로 분리하여 데이터가 있으면 항상 상시 렌더링되도록 조치.
  - Supabase Edge Function 배포 시 401 권한 에러를 방지하기 위한 `--no-verify-jwt` 배포 옵션 가이드라인 추가.
- **[Phase 8-4] Travelpayouts 화이트라벨 모달 연동 및 파트너 링크 최적화 (2026.04.11)** ⭐
  - 기존 스크롤 충돌을 유발하던 인라인 검색 위젯을 `iframe` 모달 방식으로 개편하고 CNAME 도메인 연동.
  - 반려된 숙박/교통 파트너사(아고다, 부킹닷컴, 12Go) 버튼을 삭제하고, Travelpayouts Hotellook 기반 '글로벌 최저가 호텔 통합 검색' 모달 연동을 추가.
  - **(UX 개선)** 가짜 검색 폼 UI를 제거하고 심플한 파스텔톤 단일 버튼으로 디자인 롤백/고도화.
  - 클룩(Klook) 연동 시 WAF 방화벽 차단 문제를 확인하여, 동적 딥링크가 아닌 안전한 단축 링크 우회 적용.
  - 글로벌 공항 픽업 커버리지 보완을 위해 **Intui.travel 파트너 신규 연동** 및 리액트 검색 위젯(`IntuiWidget`) 컴포넌트 탑재.
  - AI 조언 텍스트 내 범용 키워드로 인한 비자 링크 오인 매칭(할루시네이션) 방지를 위해, 순수 목적지 국가명 기반 매칭 로직으로 개선.
  - `platform-roadmap-2026.md` 내에 "AI 입국 정책 모니터링 시스템(Phase 5)" 아키텍처 초안 추가.
- **[Bug Fix] 여행지 탭 직접 접근 시 홈으로 튕기는 라우팅 오류 수정 (2026.04.12)** ⭐
  - `src/pages/Home/index.jsx` 내 `matchPath` 로직이 `/place/:slug/:tab` 구조를 인식하지 못하던 문제 해결 (탭 주소 직접 붙여넣기 시 정상 렌더링되도록 조치).
- **[UI Fix] 탐색 창 헤더 컴팩트 디자인 개선 (PC 전용) (2026.04.10)** ⭐
  - PC 버전 스크롤 반응형 헤더 구현: 100px 이상 스크롤 시 1단 헤더가 완전히 숨겨지고 2단 헤더가 최상단으로 이동 (`max-h-0`, `py-0`).
  - 1단 헤더 크기 25% 축소: 버튼(48px → 36px), 검색바(56px → 40px), 필터 버튼 패딩 축소 및 텍스트 간소화.
  - 검색바 시인성 50% 향상: 배경/테두리 명도 증가, 플레이스홀더/아이콘 색상 개선, 포커스 효과 강화.
  - 2단 헤더는 PC에서 항상 불투명 배경 (`bg-[#0b101a]`) 유지로 본문과 완전 분리.
  - 테마 버튼에 카테고리별 고유 색상 적용 (휴양-시아ン, 자연-그린, 도심-퍼플, 문화-옐로우, 모험-레드).
  - 모바일 디자인 완전 보존 (`md:` breakpoint 이상만 적용).
- **[UI Fix] 위키탭 다크모드 볼드체 텍스트 가독성 개선 (2026.04.10)** ⭐
  - `CopyableText.jsx`에서 `type` prop을 활용해 다크모드(위키)와 라이트모드(플래너) 환경에 맞춰 볼드체(`**`) 마크다운의 색상을 조건부로 렌더링(흰색/어두운 회색)하도록 분기 처리 완료.
- **[Phase 8-3] SEO 및 제휴 심사 봇 대응을 위한 index.html 정적 콘텐츠 삽입 (2026.04.09)** ⭐
  - CSR 한계 극복을 위해 `index.html`의 `<div id="root">` 내부에 숨김 처리(CSS `clip`)된 봇 전용 SEO 텍스트 추가.
  - Travelpayouts 심사 승인을 위해 파트너사 제휴 내용, 여행 가이드 설명, 그리고 테마별/대륙별 탐색 딥링크(`/explore/...`) 하드코딩 반영.
- **[Phase 8-3] Planner Hub 모바일 브릿지 및 파싱 충돌 수정 (2026.04.09)** ⭐
  - `PlannerTab.jsx`의 `getMultiLinks()` 함수 확장으로 공항 픽업 및 페리 예약 시 Klook/12Go 제휴 링크 연동 추가.
  - 마크다운 볼드체(`**...**`) 내에 스마트링크(`[@이름@]`) 문법이 중첩될 때 발생하는 UI 깨짐 현상을 2-Step 파싱 아키텍처(볼드체 우선 파싱 후 스마트링크 분리)로 완전히 해결.
  - 가독성을 저해하는 AI 생성 백틱(``` ` ```) 기호 일괄 제거 정규식 적용.
- **[Phase 8-3] Planner Hub 버그 수정 및 모바일 UI 개편 (2026.04.09)** ⭐
  - `usePlannerData.js` 내 캐시 조기 리턴 시 발생하는 무한 로딩 버그 수정.
  - 플래너 탭 활성화 시 모바일 환경에서 하단 푸터를 제거하고 네비게이션을 "앱으로 전체 일정 보내기" 버튼으로 교체.
- **[Phase 8-3] 마크다운 파싱 고도화 및 스마트 링크 연동 완료 (2026.04.09)** ⭐
  - `PlannerTab.jsx` 정규식을 고도화하여 AI 응답의 지저분한 혼합 기호 방어 및 모든 리스트/소제목을 하이픈(`-`)으로 깔끔하게 통일.
  - `update-place-toolkit` 백엔드 프롬프트 개선을 통해 AI가 핵심 명소/업체명을 `[@이름@]` 형태로 생성하도록 강제하여, 프론트엔드(`CopyableText.jsx`)와 연동된 스마트 예약 링크 자동 생성 로직 완성.
- **[Phase 8-3] Planner Hub 앱 브릿지 연동 및 백엔드 프롬프트 개편 (2026.04.09)** ⭐
  - `update-place-toolkit` Edge Function 프롬프트를 기존 서술형에서 Markdown 기반 실용적 체크리스트/꿀팁 형태로 개편.
  - `PlannerTab.jsx`의 복잡한 11가지 컬러 테마를 단순화(default, warning, danger)하여 시인성 극대화.
  - 반응형 앱 브릿지 액션 바(PC 상단 우측, 모바일 하단 고정) UI 추가 및 사용자 피드백 반영(버튼 디자인 파스텔 톤 유지).
- **[Phase 8-3 & 9] Planner Hub(스마트 트래블 플래너) 기획 완료 및 검토 (2026.04.10)** ⭐
  - 단순한 복잡한 여행지 필터링을 넘어 툴킷을 완벽한 여행 준비 허브로 격상하기 위한 `plans/toolkit-phase8-planner-hub-plan.md` 기획 수립 및 적용 완료.
  - 검색 모달 내의 플래너 뱃지 UI 기획은 오버엔지니어링 이슈로 최종 보류(취소) 처리하여 간결성 유지.
- **[Phase 8] 여행 플래너 데이터 무한 갱신 및 Race Condition 버그 수정 (2026.04.08)** ⭐
  - 탭 네이밍 변경(`Toolkit` -> `Planner`) 누락분에 대한 `usePlannerData.js` 의 조건 오류 완벽 수정.
  - React 18 렌더링 시점 불일치로 발생하던 AI 자동 재생성 Race Condition 오류를 Render Phase State Update 기법으로 차단하여 불필요한 AI 호출 100% 방지.
- **[Phase 2.1] `/explore` 모달 방식 정적 라우트 구현 (2026.04.08)** ⭐
  - 3D 지구본 렌더링 성능 유지를 위해 `/explore` 페이지 분리 방식을 폐기하고, 기존 `SearchDiscoveryModal`에 딥링크를 지원하도록 롤백 적용.
  - `App.jsx` 내 `/explore/:filter1/:filter2` 자식 라우트 추가로 대륙+테마 조합의 공유 링크 완벽 지원.
  - 모달 내 썸네일 컴포넌트(`SpotThumbnailCard`) 재활용으로 Unsplash API 중단에 따른 버그 해결.
  - 검색 결과 카드 선택 시 `PlaceCard` 모달로 안정적 진입하도록 충돌 로직(Race Condition) 제거.
- **[Phase 1] GA SPA 추적 및 SEO 내부 링크 확장 (2026.04.08)** ⭐
  - `App.jsx` 내 `useLocation` 기반 GA4 페이지뷰 추적(SPA) 연동 완료
  - `PlaceCardExpanded.jsx` 내 탭(위키, 리뷰 등) 전환 시 `tab_view` 이벤트 로깅 적용
  - 홈 화면 최하단에 SEO 크롤링 봇 전용 숨김 링크(`TRAVEL_SPOTS` 딥링크 전체 및 향후 생성될 Explore 라우트) 삽입 완료 및 Key 중복 렌더링 에러 수정
- **[Phase 8] 복잡한 여행지 특화 시스템 코어 + 버그 수정 완료 (2026.04.05)** ⭐
  - 하이브리드 모델 라우팅 분리 (위키: Gemini 2.5 Pro / 툴킷: Gemini 3.1 Pro → 2.5 Pro 폴백)
  - 툴킷 전용 `essential_guide` JSON 컬럼 마이그레이션 (성능/비용 최적화를 위해 위키 단일 소스 원칙에서 분리)
  - `PreTravelChecklist`, `JourneyTimeline` 등 확장 UI 도입 및 갱신 지연(Race Condition) 완벽 해결

- **[Phase 2.2-2.3] PlaceCard 탭 네이밍 변경 및 Path 기반 라우팅 구현 (2026.04.08)** ⭐
  - 탭 네이밍 변경 (`Logbook` -> `Reviews`, `Toolkit` -> `Planner`) 및 관련 컴포넌트/훅 파일명 일괄 변경.
  - 기존 쿼리 파라미터(`?tab=...`)를 제거하고 `/place/:slug/:tab` 형태의 Path 기반 라우팅 도입으로 탭별 SEO 색인 최적화.
  - `vite-plugin-sitemap`을 통해 1,200개 이상의 정적/동적 URL 사이트맵 생성 로직 반영.
  - 카카오 프로필 등 `http://` 이미지 로드 시 발생하는 Mixed Content 경고 수정을 위해 강제 `https://` 변환 로직 추가.

- **[Phase 8-4] 클룩(Klook) 다이렉트 어필리에이트 동적 딥링크 연동 완료 (2026.04.14)** ⭐
  - 클룩 공식 파트너 검색 위젯(iframe) 도입을 테스트했으나, 브라우저 보안 제약(Same-Origin Policy) 및 외부 검색어 파라미터 자동 기입 미지원 이슈를 확인.
  - 이를 대체하기 위해 Travelpayouts를 경유하지 않는 **클룩 다이렉트 어필리에이트 URL(aff_adid=1256120)** 구조를 도입.
  - `PlannerTab.jsx`의 클룩 연동 버튼(교통/패스, 유심/와이파이, 페리)에 각 장소명(`location.name`)이 포함된 Klook 검색 결과 페이지를 동적 딥링크로 조립하여, 방화벽(WAF) 차단 없이 즉각적이고 안정적인 검색어 자동 맵핑을 구현 완료.
- **[Phase 8-4] 모빌리티(공항 픽업/렌터카) 제휴 위젯 롤백 및 동적 딥링크 개편 (2026.04.14)** ⭐
  - 공항 픽업 위젯(Intui.travel, GetTransfer)이 외부 파라미터 주입 및 한국어 강제 적용을 지원하지 않아 발생하는 UX 저하 문제를 확인 후 위젯 컴포넌트를 모두 삭제.
  - 공항이 없는 섬 지역(예: 길리메노)에서 엉뚱한 검색 결과가 노출되는 오류를 방지하기 위해, 검색어를 강제 주입하지 않고 사용자가 직접 공항을 검색할 수 있는 클룩 "공항 교통편 전용 홈" 및 "렌터카 전용 홈" 다이렉트 딥링크 버튼 2종으로 분리 교체.

### 📦 이전 주요 작업 내역 (Archive)
- **Phase 9 (UX 최적화 및 검색 시스템)**: 검색 디스커버리 모달 신설 (아코디언 트리 UI, `usePlaceGallery` 연동). 200개 여행지 데이터 구조 1차 확장, `showOnGlobe` 최적화. 매거진 레이아웃 도입 및 Mapbox 3D 지형 지도 통합. 딥링크 방식 변경에 따른 대규모 SEO 최적화 (동적 sitemap, Canonical 방어, robots.txt).
- **Phase 7 (툴킷 로딩/UI 최적화)**: 로딩 동기화 버그 수정, 툴킷 직진입 로직. 갤러리 큐레이션 버튼 제거 등 UI 단순화.
- **Phase 4~6 (기반 최적화)**: 위키 본문 스마트 아웃링크 적용. 비로그인 사용자 가입 유도 UI. `update-place-wiki` 갱신 템플릿 로직.
- **코어 UI & 시각화**: 80개 3D 지구본 마커 균일화 및 최적화, 영문 `slug` 기반 라우팅.
- **이미지 갤러리**: Pexels/Unsplash API 연동 및 사진 누적(Append) 페이지네이션.
- **리뷰 시스템**: `place_reviews` DB 신설 및 사용자 리뷰 연동.

### 🚀 남은 과제 (Next Steps)
- [x] **[Phase 8-9] 누락된 여행지 25개 travelSpots.js에 추가** (완료) ✅
  - 25개 전체 추가 완료: ID 341-365
  - 검증 완료: 220개 → 245개, 누락 0개
  - 커밋: `98b376f`
- [ ] **[Direct Ferries Phase 2] 추천 노선 데이터 확장** (향후)
  - 일본 내륙 페리 (히로시마-미야지마, 오키나와 등) 추가
  - 북유럽 페리 (덴마크, 노르웨이, 스웨덴) 추가
  - 지중해 추가 노선 (튀르키예, 몰타 등) 검토
- [ ] **[Phase 8-6] 링크 전략 고도화 및 파트너사 전환**: 다음 세션에서 작동하지 않는 링크(에어비앤비 등)를 파악하고 트립닷컴/익스피디아 등으로 전환 적용.
- [ ] **[Phase 8-3 & 9] 복잡한 여행지 시스템 연동 (검색 모달 큐레이션)**
- [ ] **[Phase 9-2] 여행지 데이터 100개 추가 (Phase 2 대기)**
- [ ] **[Phase 10] 백엔드 프롬프트 개선 (DB 공항명 필드 추가 등) 및 A/B 테스트 검증**
- [ ] **[Future] 브라우저 이미지 압축 및 개인화 시스템(Import, 알림) 도입**

---

## 6. 📁 문서 및 로그 관리 규칙

### 로그 파일 관리
**누적 진행 로그**: `plans/YYYY-MM-DD-project-log.md`
- 일별 세션 작업 내용을 지속 업데이트 (1,000줄 또는 50KB 초과 시 새 파일 생성)
- 커밋 해시, 변경 파일 경로, 예상 효과 필수 포함
- 🔗 **이전 로그 연계 규칙**: 새로운 일자(YYYY-MM-DD)의 로그 파일을 처음 생성할 때는, 파일 최상단에 직전 로그 파일에 대한 링크(`[⬅️ 이전 로그 보기 (YYYY-MM-DD)](./이전로그파일명.md)`)를 삽입하고, 직전 로그 파일 하단의 `고려사항`이나 `Next Steps` 항목을 읽어와 오늘의 작업 계획이나 참고 사항으로 자동 이관(Carry-over)할 것. 이를 통해 맥락이 끊기지 않고 자연스럽게 이어지도록 합니다.

### 컨텍스트 파일 압축 규칙
**압축 기준**: 500줄 또는 30KB 초과 시 (최근 수정 사항 10개 초과 시 오래된 항목은 단락으로 묶어 Archive 처리)

---

## 7. 🤖 Claude(Roo) 특화 설정

### 세션 종료 프로세스 (필수)
모든 세션 종료 전 반드시 수행:
1. **커밋 완료 확인**: `git commit -m "작업 내용 요약"`
2. **로그 파일 업데이트**: `plans/YYYY-MM-DD-project-log.md`에 오늘 작업 기록
3. **컨텍스트 파일 업데이트**: `.ai-context.md`의 "최근 수정 사항" 섹션 갱신 및 필요시 압축
4. **attempt_completion으로 종료**: `📌 다음 세션 시작 제시어: ".ai-context.md와 [로그 파일] 확인하고 [다음 작업]"` 제공

### 금지 패턴
- ❌ 일회성 보고서 파일 생성 (`-completion-report.md` 등 금지, 로그 파일에 누적)
- ❌ BEFORE/AFTER 코드 전체 복사 (토큰 낭비 방지)
- ❌ 작업 예상 시간 단정 (대신 작업 항목 나열 및 진행 로그 업데이트에 주력)
