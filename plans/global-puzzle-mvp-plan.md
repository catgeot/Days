# 범지구적 퍼즐 — MVP 구현 핸드오프

**상태**: 제품 합의 ✅ · **`/play/geo` 전용 페이지 tip** (Preview QA 대기)  
**제시어**: `범지구적퍼즐-이어하기` · `@plans/global-puzzle-mvp-plan.md`  
**일지**: [`2026-07-31-project-log.md`](./2026-07-31-project-log.md)

---

## 0. 아키텍처 확정 (2026-07-31 피드백)

| | |
|--|--|
| **진입** | 홈 → `Link` `/play/geo` (**홈 모드 토글 금지**) |
| **글로브** | `PlayGeo/GeoPuzzleGlobe` 전용 — 지명·admin 라벨 **숨김** · 장소카드/탐색 클릭 **없음** |
| **홈 격리** | `HomeGlobeMapbox` / `Home/index`에 퍼즐 상태·클릭 가드 **넣지 않음** (홈 회귀 위험) |
| **루프** | 권역 → 나라 → 찾기(B) → 수도 4지선다 → 별/재도전 (합의안 유지) |

**폐기**: 홈 지구본 위 게임 모드 오버레이 (모드 인지 어려움 · 지명/장소카드 간섭 · 홈 로직 얽힘).

---

## 0.1 다음 세션 — 읽을 것 / 금지

**읽을 것 (이 순서)**

1. 본 파일 §0·§1~§4
2. [`.ai-context.md`](../.ai-context.md) — **§3 금지 · §4.1 UI**만
3. `src/pages/PlayGeo/` · `src/pages/Home/lib/globalPuzzle/`

**읽지 말 것 (전반 파악 생략)**

- `travelSpots.js` / `travelSpotAirports.json` 전체
- 명소·항공·축제 운영 가이드 전문
- 홈 `HomeGlobeMapbox` 전면 리팩터

**금지**

- 퍼즐을 다시 **홈 모드**로 이식
- 홈 탐색 UX **리디자인** — 진입은 Link만
- 테마(`primaryCategory`)로 스테이지 나누기 — 면 = **지리 권역**
- 전 세계 수도 일괄 완성 전에 막기 — **시드 소량**
- 계정·랭킹·멀티 · 세부 지명 본편

---

## 1. 제품 한 줄

**`/play/geo` 전용 페이지**에서 권역 → 나라로 도장 깨기.  
한 나라 = **찾기 탭(B) + 수도 객관식** 한 세트.  
완료 후에도 **다시 도전**. 힌트(A 포커스)는 선택이며 **별 −1**.

---

## 2. 한 세트 (확정)

| 단계 | 동작 | 비고 |
|------|------|------|
| 1 메뉴 | 권역 면 · **나라 선택**(시드) | 게임 페이지 자체 UI · 홈 레일 복제 금지 |
| 2 찾기 | 권역이 **넓게** 보인 채 목표 나라를 **탭** (B) | 시작 시 나라로 포커스·하이라이트 **하지 않음** |
| 3 수도 | **4지선다** 객관식 | 개수는 이후 설정 확장 가능하게 |
| 4 결과 | 도장 · bestStars · **다시 도전** | 세부 지명은 클리어 후 **해금 슬롯만**(본편 후속) |

**힌트 (A)**

- 버튼「힌트」→ 해당 나라 **fitBounds / 국경 하이라이트** (전용 글로브 레이어)
- 사용 시 **별 −1** (만점 3 → 최대 2)
- 기본 플레이에 힌트 자동 적용 금지

---

## 3. 별 규칙 (확정)

| | |
|--|--|
| 만점 | **별 3** — 탭 성공 + 수도 정답 + 힌트 없음 + (오답 패널티 없음) |
| 힌트 1회 | **−1** |
| 오답 후 성공 | **−1** (탭·수도 합쳐 **최대 1회** — 연속 깎기 금지) |
| 하한 | 클리어 시 **최소 별 1** (0으로 비우지 않음) |
| 재도전 | bestStars만 갱신(높은 쪽 유지). 완료 잠금 없음 |

의도: 별을 **늘리기보다**, 깎임이 **과하지 않게**.

---

## 4. MVP 범위

**In**

- 라우트 `/play/geo` · 홈 진입 Link만 (축제 버튼 아래 · 기존 톤)
- 전용 글로브(지명 숨김) · 권역 → 나라(시드) → B 탭 → 수도 4지선다 → 결과·재도전
- 진행 저장: **localStorage** `gateo_global_puzzle_v1`
- 수도 시드 소량 (`globalPuzzle/capitalsSeed.js`) · catalog id 조인
- 오답 재시도 · 다시 도전 OK

**Out (후속)**

- 홈 지구본 모드 이식
- 세부 지명 본편 · 실루엣 드래그 부활(선택)
- 서버 진행·리더보드 · 전 카탈로그 수도

---

## 5. 기술 앵커

| 역할 | 파일 |
|------|------|
| 페이지 | `src/pages/PlayGeo/index.jsx` |
| 전용 글로브 | `src/pages/PlayGeo/GeoPuzzleGlobe.jsx` (라벨/admin 숨김) |
| 탭 판정 | `src/pages/PlayGeo/lib/findCountryTap.js` |
| 별·세션·시드·진행 | `src/pages/Home/lib/globalPuzzle/*` |
| 권역 시드 면 | `GLOBE_FACE_PRIORITY` (`globeFaceRegions.js`) |
| 나라 catalog | `globeCountryCatalog.js` |
| 라우트 | `App.jsx` → `/play/geo` |
| 홈 진입 | `HomeUI.jsx` → `Link to="/play/geo"` 만 |

**수도 distractor**: 같은 면 시드 우선 · 부족하면 전체 시드.

---

## 6. VERIFY

```bash
npm run smoke:global-puzzle
npm run build
```

---

## 7. QA 체크리스트 (사람 Preview · `/play/geo`)

- [ ] 홈「범지구적 퍼즐」→ `/play/geo` 이동 (홈 모드 토글 아님)
- [ ] 게임 글로브에 **지명 라벨 없음** · 탭해도 **장소카드 안 뜸**
- [ ] 나라 선택 → 카메라가 **그 나라로 미리 붙지 않음**
- [ ] 오답 탭 → 재시도 · 정답 → 수도 → 클리어·별
- [ ] 힌트 1회 → 포커스/하이라이트 · 별 최대 2
- [ ] 다시 도전 · bestStars 갱신
- [ ] 홈 복귀 후 탐색(숙소·나라 칩) 회귀 없음
- [ ] 모바일·PC 세트 완료 가능

---

## 8. 에이전트 제시어 (복붙)

```
범지구적퍼즐-이어하기
@plans/global-puzzle-mvp-plan.md
/play/geo 전용 페이지 유지. 홈 모드 금지. 권역→찾기(B)→수도·별.
힌트=A 포커스(별−1). 지명/장소카드 없음. 시드·localStorage.
```
