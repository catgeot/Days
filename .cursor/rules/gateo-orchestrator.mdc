---
description: "오케스트레이터 — 지휘자1+워커N(기본2). 제시어 오케스트레이터/명소-오케스트레이터/맵박스정착지/지자체팔경/팔경contentId"
alwaysApply: false
---

# 오케스트레이터 세션

사용자가 **`오케스트레이터`** · **`명소-오케스트레이터`** · **`맵박스정착지`** · **`지자체팔경`** · **`팔경contentId`** 를 쓰거나 `@plans/orchestrator-method.md` 를 연결하면 이 규칙을 따른다.  
방법 SSOT: [`plans/orchestrator-method.md`](plans/orchestrator-method.md) **v2.4.1**. Cloud **2회 하드캡 아님**. 워커는 파일+요약 · 커밋은 지휘자.

## 필수 읽기 (짧게)

1. [`plans/orchestrator-method.md`](plans/orchestrator-method.md) — **§1 · §1.3 · §3.0 · §3.3 · §3.4 · §3.5 · §3.6 · §4.2** (+ 주제면 §5 / 정착지 §5.3 / 팔경 수집 **§5.6** / contentId **§5.7**)
2. 명소: [`plans/city-attraction-hub-queue.md`](plans/city-attraction-hub-queue.md) **다음 ⬜ R** + 일지 핸드오프 1절만
3. 정착지: [`plans/mapbox-settlement-queue.md`](plans/mapbox-settlement-queue.md) **다음 ⬜ R** + [`mapbox-settlement-plan.md`](plans/mapbox-settlement-plan.md)
4. 지자체 팔경 **수집**: 종료. 큐 [`korea-local-scenic-lists-queue.md`](plans/korea-local-scenic-lists-queue.md) F 재개 **금지**
5. 팔경 contentId: 큐 [`korea-local-scenic-contentid-queue.md`](plans/korea-local-scenic-contentid-queue.md) **다음 ⬜** + [`korea-local-scenic-use-plan.md`](plans/korea-local-scenic-use-plan.md) **§9** · method **§5.7**
6. [`.ai-context.md`](.ai-context.md) — **§1.5.3** · [`gateo-commit-push.mdc`](gateo-commit-push.mdc) · 관련 금지 1~2줄

**전반 코드베이스 탐색·이전 워커 로그 전체 Read 금지.**

## 역할 (고정)

- **당신 = 지휘자**: 다음 R만 워커 N 기동(기본 2) · 워커는 `tmp/orchestrator/`+요약 · **당신이 직렬 머지·VERIFY·커밋** · 컨텍스트가 찰 때까지 워커 재기동. JSON 전문 채팅 Read 금지 · 워커에게 commit/push 맡기지 않음 · 남은 R 전부 기동 금지 · Cloud 중첩 후임 금지
- **워커**: 배정 1배치를 **파일**에만. tip append · **commit/PR/push** · JSON을 부모 채팅에 붙임 · SSOT rewrite · 이관서 · 자기 아래 Task **금지**
- **후임 지휘자**: **워커 N을 직접 기동**할 수 있는 깊이여야 함. Cloud = 같은 메인 연장 또는 **새 Cloud 런**(중첩 Task 후임 **금지**). Desktop = 컨트롤러가 다음 지휘자 L1 기동.  
  기본 **본인 런 금지**. 예외는 워커 1회 재시도 실패 시만 · 일지에 사유 1줄.

## 커밋·PR (§1.5.3 · method §3.4)

- 게이트 = **VERIFY PASS**. PASS면 **허가 요청 없이** 커밋·(Cloud) push · 「커밋해도 될까요」 **금지**.
- 시점: 매 라운드 VERIFY 직후(권장) · **이관·턴 종료·사람 보고 전(필수)**.
- **Cloud**: 커밋 + **feature push** + PR(없으면 생성). **`origin/main` push는 사람 요청 시만**.
- **로컬**: tip 커밋 필수 · feature/PR push는 핸드오프·Preview 시.
- **금지**: VERIFY FAIL tip 커밋 · `origin/main` 에이전트 push · 워커가 commit/PR.

## 이관 = 지휘권 이양 (사람 제시어 대기 금지)

- **Cloud**: 지휘자 = 이 런의 메인. 워커 = L1. VERIFY 후 **같은 지휘자가 워커 N 재기동**(연장 · **2회에서 멈추지 않음**). **중첩 후임 Task 금지**(후임이 워커를 못 띄움).
- **Desktop**: 컨트롤러(사람 채팅)가 다음 지휘자 L1을 띄움. 지휘자 L1이 워커 L2 기동. 지휘자가 후임을 직접 띄우면 L3라서 불가.
- **금지**: VERIFY 후 「다음 제시어 대기」만 하고 정지.
- **금지**: 워커를 백그라운드로 띄운 뒤 **초안 수신·머지·VERIFY 전에 턴 종료**/조기 `final_summary`.
- 사람이 제시어를 넣는 경우: 최초 착수 · §3.3 E/사용자 중단 후 복구 · Cloud 런 사망 시에만.
- 파이프 단절: tip 미반영·`_tmp*` 잔여면 같은 지휘자가 워커 N 루프 재개.

## 금지 (재발 방지)

- 배치 1개 끝내고 바로 이관하는 **솔로 계주**
- Task만 띄우고 **tip 미append**
- tip **병렬** 머지
- VERIFY 실패 tip을 남긴 채 다음 R·이관·commit
- VERIFY PASS인데 **커밋 없이** 이관·턴 종료 (Cloud면 PR도)
- 워커에게 commit/push를 맡김 · JSON 본문을 부모 채팅에 붙임 · 남은 R를 한 번에 전부 기동
- Cloud에서 중첩 후임 Task에게 워커 기동을 맡김
- (명소) 큐 밖 임의 지명 · KR 구·DE/UK 중소로 새기

## 문제 조치 (요약 · 상세 method §3.3)

- **매 라운드** audit+스모크 VERIFY · issues>0/FAIL → append분 **부분 제거** 또는 직전 VERIFY tip 복귀 후 재VERIFY
- 워커B 실패: tip엔 **VERIFY PASS분만** · B는 재시도1 → 예비/다음 턴
- 사용자 중단: audit·건수·tmp 잔여·큐 R 상태 체크 후 재개/보고
- 사람한테 물음: 롤백 후에도 FAIL · 시드 손상 · 규칙 변경 필요 · UI/releaseNotes · 동일 FAIL 2회

## 병렬/직렬

- 워커 N 초안: **병렬 필수** (기본 2 · §3.5에서 N 증가 가능)
- 동일 tip append: **직렬만**
- Cloud 이관: **같은 지휘자 연장** · 중첩 후임 체인 금지

## 명소 hub

- SSOT: `cityAttractionHubs.json` append only · `shrine` 유지 · 시드 덮어쓰기 금지  
- 게이트: `npm run audit:city-attraction-hubs` (+ 좌표: `audit:city-attraction-coords` · `verify:city-attraction-coords`)  
- 지명: **큐 순서만** (`city-attraction-hub-queue.md`) · 라운드=A5+B5 · 임의 선택 금지  
- **좌표 수리**: method **§5.4** · 제시어 `명소좌표수리` · NAMED는 feature 스냅(+`mapboxId`) · KR hub 추정 금지  

## Mapbox 정착지

- SSOT: `mapboxSettlementPlaces.json` · hub당 1행 · `place|city|locality` only · POI 금지  
- 개수: 목표3 · 최대5 · 최소2 · 미달 스킵 · 시드 `sokcho`/`paris` 덮어쓰기 금지  
- 게이트: `npm run audit:mapbox-settlement-places` + `smoke:mapbox-settlement-places`  
- 큐: [`plans/mapbox-settlement-queue.md`](plans/mapbox-settlement-queue.md) 순서만 · hub 밖 임의 금지  

## 지자체 팔경·구경 (§5.6)

- SSOT: `koreaLocalScenicLists.json` + hub **append·aliases만** · 기존 명소 삭제/교체 금지  
- 단위: **시·군·구** · 광역 팔경 1차 금지 · UI·`koreaScenicSpots` 자동승격 금지  
- 라운드: A3+B3=6 · 출처 공식 URL 필수 · 큐 [`korea-local-scenic-lists-queue.md`](plans/korea-local-scenic-lists-queue.md)  
- 게이트: `audit:korea-local-scenic-lists` + `audit:city-attraction-hubs` · 파일럿 후·3R마다 **I 무결성**  
- 제시어: `오케스트레이터` + `지자체팔경` — **수집 종료 · F 재개 금지**

## 팔경 Tour contentId (§5.7)

- SSOT: 팔경 멤버 `contentId` + hub attraction 복사 · scenic 승격 금지 · UI 금지  
- **P0 팔경 먼저** · DB(`tourapi_attraction`) 우선 · LIVE는 잔여만 · **워커 병렬 LIVE 금지**  
- 429 → 큐 `blocked: quota` · 같은 날 재시도 금지  
- 큐: [`korea-local-scenic-contentid-queue.md`](plans/korea-local-scenic-contentid-queue.md) · S0 후 F  
- 제시어: `오케스트레이터` + `팔경contentId`

## 세션 종료(체인 끝)

큐 소진·§3.3 E·사용자 중단 시에만. 마지막 VERIFY PASS tip도 §3.4 커밋(+ Cloud PR) 후 일지에 VERIFY·건수·SHA·PR·다음 배치표 2개·복구용 제시어 2~5줄. `.ai-context` 5절은 스냅샷 1줄만.
